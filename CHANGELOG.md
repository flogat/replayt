# Changelog

## Unreleased

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.25`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.25 - 2026-03-23

- **Reviewcodebase pass:** **`JSONLStore._read_last_seq_locked`** treats JSON object tail lines with a non-coercible **`seq`** (wrong type or non-numeric string, including JSON **`null`**) like a bad tail: scan earlier lines or fall back to **`_max_seq_full_scan`**, instead of raising **`TypeError`** / **`ValueError`** during **`append_event`** / mirror **`append`**. **`_max_seq_full_scan`** uses the same coercion helper for consistency. Implemented in [`src/replayt/persistence/jsonl.py`](src/replayt/persistence/jsonl.py); regressions in [`tests/test_jsonl_store.py`](tests/test_jsonl_store.py).

- **Docs:** deslopdoc pass on [`docs/CLI.md`](docs/CLI.md) (colon instead of em dash before the validate JSON note; **inspect** / **runs** wording: `for support-oriented …`, `matching` handoff commands), [`docs/CONFIG.md`](docs/CONFIG.md) (`matching` instead of "aligned with" / "aligned to" for version JSON contracts), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md) (`matches` for `typical_exit_code`; report-diff handoff phrasing).

- **Improvedoc pass:** **`OpenAICompatClient.chat_completions`** records **`_http_correlation_breadcrumbs`** ( **`x-request-id`** / **`x-correlation-id`**, **`openai-processing-ms`**, **`cf-ray`**) while the **`httpx`** streaming response is still open, then merges that snapshot into **`transport_meta`** after the body is decoded, instead of reading **`r.headers`** after the **`with`** block ends. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).

- **Design fidelity:** **`jsonl_event_types_contract`** on **`replayt version --format json`** now includes **`run_interrupted`** (parent **`replayt run --timeout`** append) so the machine-readable event-type list matches first-party JSONL writes; contract **`notes`** clarify that the list covers core emissions plus first-party CLI append paths (not only **`resume`**). Documented under **`run_interrupted`** in [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Agent harness / automation:** Repo-relative skill-loop paths (**`SKILL_PROMPT_REL`**, **`SKILL_LOG_REL`**, **`SKILL_INVOCATION_REL`**, **`SKILL_RUN_DIR_REL`**, matching **`{prompt_rel}`** / **`{log_rel}`** / **`{invocation_rel}`** / **`{run_dir_rel}`** in **`--skill-command`** templates, and **`prompt_file_rel`** / **`log_file_rel`** / **`run_dir_rel`** on **`replayt.skill_invocation.v1`**) now use **POSIX forward slashes on every platform** (including Windows), so **`jq`** filters and cross-OS artifact keys align with Linux CI. **`path_under_repo_or_absolute`** in **`scripts/skill_release_loop.py`** and **`scripts/skill_release_loop_agent.py`** implements the rule; **`skill_loop_env_contract`** and **`skill_loop_placeholder_contract`** descriptions note it. Documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py) and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).

- **MCP / tooling:** Each row under **`cli_json_stdout_contract.subcommands`** now includes **`positional_required`** and **`positional_optional`**: ordered Typer positional slot names (**`TARGET`**, **`RUN_ID`**, **`RUN_A`**, **`RUN_B`**) so subprocess and MCP hosts can wire argv arity without Typer introspection; optional **`TARGET`** may still resolve from env or project config (**`cli_run_defaults_contract`**). **`replayt try`** JSON routes omit **`TARGET`** (the packaged example supplies the workflow). Compose-outside **embedded JSON Schema bodies on `replayt version` JSON** and **MCP `resources/list` for live run logs** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md). Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`ToolRegistry.openai_chat_tool_result_messages(tool_calls, results)`** builds OpenAI Chat Completions **`role: "tool"`** message dicts parallel to **`apply_openai_chat_tool_calls`**, using each vendor **`id`** as **`tool_call_id`** and JSON-encoding non-string tool returns (including **`BaseModel.model_dump_json()`**). Compose-outside **OpenAI Assistants v2 thread/run lifecycle in `Runner`** and **Hugging Face smolagents auto-wrapping every registered tool** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: OpenAI Assistants threads and smolagents-style tool loops** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md). Implemented in [`src/replayt/tools.py`](src/replayt/tools.py); documented in [`README.md`](README.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_tools.py`](tests/test_tools.py).

- **Enterprise integrator:** **`REPLAYT_RUN_STARTED_INITIAL_STATE`** on trusted policy hooks (**`run_hook`**, **`resume_hook`**, **`export_hook`**, **`seal_hook`**, **`verify_seal_hook`**): plain string from **`run_started.initial_state`** when the first **`run_started`** line is readable (**`""`** when the key is absent), or on a brand-new run from the loaded workflow's initial state when set (**`run_hook`** only). Omitted when **`run_started`** is missing or unreadable on **`--resume`** and when a brand-new workflow has no initial state. **`policy_hook_env_catalog`** lists the name on all five hooks. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt validate … --print-ci-one-liner`** (text mode only) prints one POSIX-quoted shell line after a successful graph check: **`replayt ci`** with the resolved **`TARGET`**, compact **`--inputs-json`** from the same **`@wf.step(expects=…)`** placeholder union as **`--print-inputs-template`**, and **`--strict-graph`** when validation used **`--strict-graph`**. Mutually exclusive with **`--format json`**, **`--print-inputs-template`**, and **`--print-run-one-liner`**. Implemented in [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md) and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** **`python scripts/check_docs_index.py --format json`** (**`replayt.docs_index_report.v1`**) now includes **`expected_docs_targets_sha256`** and **`indexed_docs_targets_sha256`**: UTF-8 SHA-256 hex digests of canonical JSON for the sorted **`expected_docs_targets`** and **`indexed_docs_targets`** path lists, so fork CI can spot a new top-level **`docs/*.md`** file or a change in **`docs/README.md`** index coverage without comparing the full arrays. Implemented in [`scripts/check_docs_index.py`](scripts/check_docs_index.py); documented in [`CONTRIBUTING.md`](CONTRIBUTING.md), [`README.md`](README.md) (**Forks and release hygiene**), and **Beyond core: Unreleased text on `replayt version` JSON and changelog auto-formatters** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **towncrier-style changelog fragments owned by upstream** and **a bundled `replayt[all]` optional extra** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).

- **Product engineer:** **`replayt runs --output markdown`** prints a paste-friendly Markdown inventory (filtered rows with **`attention_summary`**) plus the same **Stakeholder CLI handoff** copy-paste templates as single-run reports, using a literal **`RUN_ID`** placeholder and optional **`--style support`** (with **`--output markdown`**) for support-aligned **`replayt report` / `replay` / `bundle-export`** commands. Empty listings still emit the Markdown scaffold and stderr hints. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py) and [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt.ci_run_summary.v1`** (from **`replayt ci --summary-json`**, **`replayt run`** with **`REPLAYT_SUMMARY_JSON`**, or env **`REPLAYT_SUMMARY_JSON`**) now includes **`python_utf8_mode`** (**`sys.flags.utf8_mode`**, **0** or **1**) and **`os_cpu_count`** (**`os.cpu_count()`** or **`null`**) alongside the existing interpreter and host stamps, so archived summaries distinguish UTF-8 mode (Windows CI and **`PYTHONUTF8`**) and undersized runner CPU allocation without coupling to a single CI vendor. Implemented in [`src/replayt/cli/ci_artifacts.py`](src/replayt/cli/ci_artifacts.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **Azure Pipelines `##vso[task.logissue …]` from `replayt ci`** and **GitLab CI `section_start` / `section_end` wrappers around `replayt ci` output** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: GitHub Actions workflow commands and `pip check` beside `replayt doctor`** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** Successful OpenAI-compat chat completions now copy bounded **HTTP response** correlation fields from the winning POST into **`transport_meta`**, then onto **`llm_response`** and **`structured_output`**: **`http_response_request_id`** (from **`x-request-id`** or **`x-correlation-id`**), **`http_response_processing_ms`** (from **`openai-processing-ms`** when parseable), and **`http_response_cf_ray`** (from **`cf-ray`**). Omitted when headers are absent, oversize, or invalid. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); documented in [`README.md`](README.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/RECIPES.md`](docs/RECIPES.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); compose-outside **built-in temperature / sampling grid runs inside `Runner`** and **auto-injecting Pydantic `Field` / JSON Schema `examples` into `parse` system prompts** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: temperature grids and implicit parse prompt stuffing** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).

- **Security / compliance:** On **POSIX** hosts, **new** SQLite mirror databases created when the CLI builds **`--sqlite`** stores (**`make_store`**) are **`chmod`**'d to **owner read/write only (`0600`)** after initial schema setup, using the same **`REPLAYT_JSONL_POSIX_MODE`** resolution as **`JSONLStore`** (**`inherit`** / octal override). **`SQLiteStore(..., posix_new_db_file_mode=None)`** skips **`chmod`** for **`Runner.run`** call sites that need umask-only semantics. Implemented in [`src/replayt/persistence/sqlite.py`](src/replayt/persistence/sqlite.py) and [`src/replayt/cli/stores.py`](src/replayt/cli/stores.py); documented in [`README.md`](README.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/CLI.md`](docs/CLI.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_sqlite_store.py`](tests/test_sqlite_store.py).

- **Junior onboarding:** Text-mode **`replayt runs`** with an empty listing still prints **`No runs found in …`** on stdout and adds stderr hints: first-run copy-paste (**`replayt try`**, **`run_id=`** from **`replayt run`**) when the log store has no run IDs yet, filter-tightening guidance when runs exist but flags excluded every row, the usual log-store parity reminder (**`REPLAYT_LOG_DIR`**, project **`log_dir`**, **`--log-subdir`**, **`--sqlite`**), and a **`replayt runs …`** line that repeats the same store flags as **`inspect`** / **`replay`**. **`--output json`** is unchanged. Implemented in [`src/replayt/cli/run_id_hints.py`](src/replayt/cli/run_id_hints.py) and [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** **`replayt version --format json`** now includes **`jsonl_event_types_contract`** (`replayt.jsonl_event_types_contract.v1`): sorted **`event_types`** with canonical JSONL envelope **`type`** strings replayt core emits, **`payload`** pointers to **`docs/RUN_LOG_SCHEMA.md`**, notes for external appenders, and a cross-reference to **`log_mode_contract`**. Documented in **`docs/CLI.md`**, **`docs/CONFIG.md`**, **`docs/RUN_LOG_SCHEMA.md`** (adds **`context_snapshot`**, **`approval_applied`**, and **`run_paused`** transition hints), and the tutorial README; regressions in **`tests/test_cli.py`**.

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.24`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.24 - 2026-03-23

- **Reviewcodebase pass:** **`JSONLStore`** sequence discovery (**`_read_last_seq_locked`**, **`_max_seq_full_scan`**) skips JSON lines that decode to non-objects (arrays, scalars) instead of raising **`AttributeError`** on **`.get`**, matching **`load_events`** object-only rules and keeping **`append_event`** / mirror **`append`** usable when the tail line is valid but not an event object. Implemented in [`src/replayt/persistence/jsonl.py`](src/replayt/persistence/jsonl.py); regressions in [`tests/test_jsonl_store.py`](tests/test_jsonl_store.py).

- **Docs:** deslopdoc pass on [`docs/SCOPE.md`](docs/SCOPE.md) (rename scope table column **Common request**; drop **You might ask for** hedging), [`docs/CLI.md`](docs/CLI.md) (describe **`inspect --output markdown --style support`** without an em dash in the **Support handoff** heading prose), [`src/replayt_examples/README.md`](src/replayt_examples/README.md) (LangGraph comment punctuation), and [`CONTRIBUTING.md`](CONTRIBUTING.md) (tighten scope sentence).

- **Improvedoc pass:** **`ToolRegistry.call`** rejects non-mapping **`arguments`** with a clear **`TypeError`** instead of failing later on **`set(arguments)`** or emitting misleading **`tool_call`** JSONL. **`SQLiteStore.append_event`** / **`append`** roll back only on **`sqlite3.Error`** (still after the dedicated **`IntegrityError`** branch on **`append`**) so non-database bugs in the **`try`** block are not masked by a broad **`except Exception`**. Implemented in [`src/replayt/tools.py`](src/replayt/tools.py) and [`src/replayt/persistence/sqlite.py`](src/replayt/persistence/sqlite.py); regressions in [`tests/test_tools.py`](tests/test_tools.py).

- **Agent harness / automation:** **`--skill-command`** templates in **`scripts/skill_release_loop.py`** and **`scripts/skill_release_loop_agent.py`** accept **`{prompt_rel}`**, **`{log_rel}`**, **`{invocation_rel}`**, and **`{run_dir_rel}`** (plus **`{name}_q`** shell-quoted variants), matching **`SKILL_PROMPT_REL`**, **`SKILL_LOG_REL`**, **`SKILL_INVOCATION_REL`**, and **`SKILL_RUN_DIR_REL`** so argv construction can stay portable without reading the environment. **`skill_loop_placeholder_contract.skill_command_placeholders`** on **`replayt version --format json`** documents the names. Implemented in [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py), [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py), and [`src/replayt/cli/skill_loop_placeholders.py`](src/replayt/cli/skill_loop_placeholders.py); documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py), and [`tests/test_skill_loop_placeholders.py`](tests/test_skill_loop_placeholders.py).

- **MCP / tooling:** Each row under **`cli_json_stdout_contract.subcommands`** now includes **`route_id`**: a stable, globally unique dot-separated string (Typer subcommand names use underscores in the first segment, for example **`verify_seal.…`** for **`verify-seal`**) so MCP **`tools/list`** keys and upgrade diffs do not depend on JSON array order. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt run` / `ci` HTTP webhook posting `run_result` on completion** and **`replayt exec-tool`-style CLI that invokes `ToolRegistry` handlers outside a workflow** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** Optional **`tool_call_id`** on JSONL **`tool_call`** and **`tool_result`** payloads when **`ToolRegistry.call(..., tool_call_id=...)`** is used, or when **`apply_openai_chat_tool_calls`**, **`apply_anthropic_tool_use_blocks`**, or **`apply_bedrock_converse_tool_use_blocks`** forwards a vendor id (OpenAI **`id`**, Anthropic **`tool_use.id`**, Bedrock **`toolUseId`**). Omits the field when no stable id is present. Implemented in [`src/replayt/tools.py`](src/replayt/tools.py); documented in [`README.md`](README.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **LangGraph `ToolNode` that mutates `RunContext` from tool return payloads** and **Ollama native `/api/chat` tool JSON helpers separate from OpenAI-compat** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: LangGraph tool nodes and Ollama tool JSON** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_tools.py`](tests/test_tools.py).

- **Enterprise integrator:** **`REPLAYT_RUN_STARTED_RUNTIME_JSON`** on trusted policy hooks (**`run_hook`** when **`replayt run` / `replayt ci`** uses **`--resume`**, plus **`resume_hook`**, **`export_hook`**, **`seal_hook`**, **`verify_seal_hook`**): canonical sorted JSON of **`run_started.runtime`** from the first **`run_started`** line when readable (**`{}`** when **`runtime`** is absent or null), so org gates can enforce **`engine.log_mode`**, **`llm.model`**, and nested contract metadata without **`jq`**. Omitted on brand-new **`run_hook`** invocations before **`run_started`** exists. **`policy_hook_env_catalog`** lists the name on all five hooks. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt validate … --print-run-one-liner`** (text mode only) prints one POSIX-quoted shell line after a successful graph check: **`replayt run`** with the resolved **`TARGET`**, compact **`--inputs-json`** built from the same **`@wf.step(expects=…)`** placeholder union as **`--print-inputs-template`**, and **`--strict-graph`** when validation used **`--strict-graph`**. Mutually exclusive with **`--format json`** and **`--print-inputs-template`**. Implemented in [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md) and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** **`python scripts/public_api_report.py --format json`** (**`replayt.public_api_report.v1`**) now includes **`export_sha256s`**: a parallel list of SHA-256 hex digests (UTF-8, one per row in **`exports`** order) from canonical JSON per export, so fork CI can detect which **`__all__`** entry drifted without comparing the entire **`docs/PUBLIC_API_CONTRACT.json`** snapshot. Implemented in [`scripts/public_api_report.py`](scripts/public_api_report.py); documented in [`CONTRIBUTING.md`](CONTRIBUTING.md); checked-in [`docs/PUBLIC_API_CONTRACT.json`](docs/PUBLIC_API_CONTRACT.json) regenerated; regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).

- **Product engineer:** **`replayt inspect RUN_ID --output markdown`** accepts **`--style stakeholder`** (default) or **`support`**: support triage gets a **`## Support handoff`** heading and copy-paste lines for **`replayt report` / `replayt replay` / `replayt bundle-export`** using **`--style support`** and **`{run_id}-support-bundle.tar.gz`** (stakeholder remains unchanged). **`--style support`** without **`--output markdown`** is rejected. **Stakeholder CLI handoff** panels on **`replayt report`**, **`replayt replay`**, **`replayt report-diff`**, and **`replayt diff --style stakeholder|support`** now repeat matching **`--style`** on paste-friendly **`replayt inspect --output markdown`** lines; **`report-diff`** / diff handoffs use consistent report/replay/bundle commands and **`support-bundle`** filenames when the comparison style is support. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py) and [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md), [`README.md`](README.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_display.py`](tests/test_display.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt.ci_run_summary.v1`** (from **`replayt ci --summary-json`**, **`replayt run`** with **`REPLAYT_SUMMARY_JSON`**, or env **`REPLAYT_SUMMARY_JSON`**) now includes **`python_cache_tag`**: **`sys.implementation.cache_tag`** when the interpreter exposes it (else **`null`**), matching the ABI hint on **`replayt version --format json`** → **`python.implementation.cache_tag`** so archived summaries distinguish CPython **`cp313`**-style tags from PyPy and other builds in mixed matrix jobs. Implemented in [`src/replayt/cli/ci_artifacts.py`](src/replayt/cli/ci_artifacts.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **GitHub Actions `::error::` / `::notice::` emission from `replayt ci`** and **`python -m pip check` inside `replayt doctor`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: GitHub Actions workflow commands and `pip check` beside `replayt doctor`** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** Successful chat completions now record optional **`http_attempts`** and **`http_status`** on **`llm_response`** and **`structured_output`** JSONL events: the OpenAI-compat client fills a caller-supplied transport bag on success (including after retryable 429/5xx backoff), and **`LLMBridge`** copies integers onto those payloads; **`OpenAICompatClient.chat_completions(..., transport_meta=...)`** is optional for custom clients. **`DryRunLLMClient`** / **`MockLLMClient`** set **`http_attempts=1`**, **`http_status=200`**. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py) and [`src/replayt/testing.py`](src/replayt/testing.py); documented in [`README.md`](README.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/RECIPES.md`](docs/RECIPES.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py) and [`tests/test_runner.py`](tests/test_runner.py).

- **Security / compliance:** **`egress_trust_env`** on **`replayt doctor --format json`** and **`llm.egress_trust_env`** on **`replayt config --format json`**: fixed sorted **`{name, present}`** rows for common proxy variables (on POSIX, lowercase **`http_proxy`**, **`https_proxy`**, **`no_proxy`**, and **`all_proxy`** count toward the canonical uppercase names) and TLS trust-store overrides (**`SSL_CERT_FILE`**, **`REQUESTS_CA_BUNDLE`**, **`CURL_CA_BUNDLE`**, **`SSL_CERT_DIR`**), mirroring **`credential_env`** for egress reviews without echoing values. Implemented in [`src/replayt/security.py`](src/replayt/security.py), [`src/replayt/cli/config.py`](src/replayt/cli/config.py), and [`src/replayt/cli/commands/doctor.py`](src/replayt/cli/commands/doctor.py); documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_security.py`](tests/test_security.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **Junior onboarding:** Invalid **`run_id`** values (paths, slashes, quotes, and other disallowed characters) now append a one-line hint to copy the id from **`replayt runs`** or the **`run_id=`** line from **`replayt run`**, after the existing JSONL basename rules. **`replayt inspect`**, **`replay`**, and **`diff`** validate **`RUN_ID`** up front and print the same stderr message as **`seal`** / **`report`** (instead of surfacing a bare **`ValueError`**). When **`inspect`**, **`replay`**, **`report`**, **`diff`**, **`export-run`**, or **`bundle-export`** find no events for a syntactically valid id, stderr adds a reminder to use the same log store as the original **`replayt run`** (**`REPLAYT_LOG_DIR`**, project **`log_dir`**, log subdirectory and SQLite options when applicable). Implemented in [`src/replayt/persistence/jsonl.py`](src/replayt/persistence/jsonl.py), [`src/replayt/cli/run_id_hints.py`](src/replayt/cli/run_id_hints.py), and [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md); compose-outside **`replayt fmt-run`** / pretty-jsonl rewrite and **`replayt copy-run-id`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: ad-hoc JSONL views and run_id clipboard** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** **`replayt version --format json`** now includes **`log_mode_contract`** (`replayt.log_mode_contract.v1`): the only canonical **`LogMode`** strings (**`full`**, **`redacted`**, **`structured_only`**) with short summaries, **`--log-mode`** / **`log_mode`**, **`REPLAYT_LOG_MODE`** on policy hooks, and a cross-reference to **`project_setting_precedence_contract`** **`log_mode`**. Registered under **`cli_machine_readable_schemas.log_mode_contract`**. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.23`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.23 - 2026-03-23

- **Reviewcodebase pass:** **`JSONLStore.load_events`** and **`SQLiteStore.load_events`** reject lines or **`payload_json`** values that decode to non-objects (for example JSON arrays or scalars) with the same **`Corrupted …`** **`RuntimeError`** shape as invalid JSON, instead of letting later CLI/report code fail with opaque **`TypeError`** / **`KeyError`**. **`OpenAICompatClient.chat_completions`** drops the unused **`last_exc`** accumulator and ends with an explicit internal **`RuntimeError`** if control ever falls through the retry loop. **`cmd_bundle_export`** renames **`mermaid_txt`** to **`mermaid_bytes`** to match **`_workflow_contract_snapshot`** (values are UTF-8 **`bytes`**). Implemented in [`src/replayt/persistence/jsonl.py`](src/replayt/persistence/jsonl.py), [`src/replayt/persistence/sqlite.py`](src/replayt/persistence/sqlite.py), [`src/replayt/llm.py`](src/replayt/llm.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); regressions in [`tests/test_jsonl_store.py`](tests/test_jsonl_store.py) and [`tests/test_sqlite_store.py`](tests/test_sqlite_store.py).

- **Docs:** deslopdoc pass on [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md): drop the "should be able to" hedge on the mental-model principle, replace em-dash breaks with semicolons or parentheses in CI summary and MCP JSON contract prose, and swap "You should see" for inspect-oriented wording on failed runs.

- **Improvedoc pass:** **`OpenAICompatClient.chat_completions`** decodes the streamed response body as UTF-8 before **`json.loads`**, turning **`UnicodeDecodeError`** into a **`RuntimeError`** that includes **`body_bytes=`** (same class of failure surface as invalid JSON) so mis-encoded or binary gateway responses do not surface as a raw decode exception. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).

- **Agent harness / automation:** **`SKILL_INVOCATION_FILE`** and **`SKILL_INVOCATION_REL`** on every **`--skill-command`** subprocess (including **`fix_check`** / **`fix_pre_tag_ci`**), plus **`{invocation_file}`** / **`{invocation_file_q}`** in templates, point at the atomic **`replayt.skill_invocation.v1`** sidecar for that step so backends and wrappers can **`jq`** metadata without deriving **`*.invocation.json`** paths from prompt stems. **`skill_loop_env_contract`** and **`skill_loop_placeholder_contract`** on **`replayt version --format json`** list the new names. Implemented in [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py), [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py), [`src/replayt/cli/skill_loop_env.py`](src/replayt/cli/skill_loop_env.py), and [`src/replayt/cli/skill_loop_placeholders.py`](src/replayt/cli/skill_loop_placeholders.py); documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py), and [`tests/test_skill_loop_placeholders.py`](tests/test_skill_loop_placeholders.py).

- **MCP / tooling:** Each row under **`cli_json_stdout_contract.subcommands`** now includes **`exit_codes_with_json_on_stdout`**: sorted exit codes that can occur **after** replayt wrote that route's declared **`schema`** JSON to stdout (for example **`[0, 1, 2]`** for **`run_result`** routes, **`[0, 1]`** for **`validate_report`**, **`doctor_report`**, **`workflow_contract_check`**, and **`verify_seal_report`**). Typer pre-dispatch failures and many pre-callback validation errors exit without JSON on stdout; see **`typer_pre_dispatch_phase`** in the same contract object. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt mcp-bridge` HTTP/gRPC multiplex** and **generated `.cursor/mcp.json` from `replayt version`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`ToolRegistry.bedrock_converse_tools()`** builds sorted Amazon Bedrock Converse API **`toolConfig["tools"]`** entries (**`toolSpec`** with **`inputSchema.json`** from the same JSON Schema derivation as **`openai_chat_tools()`**). **`apply_bedrock_converse_tool_use_blocks(...)`** walks assistant **`content`** blocks, skips non-**`toolUse`** entries, and runs each **`toolUse`** through **`call`** in list order so JSONL **`tool_call`** / **`tool_result`** events match the OpenAI and Anthropic composition paths. Implemented in [`src/replayt/tools.py`](src/replayt/tools.py); documented in [`README.md`](README.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **LangGraph (and similar frameworks)** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **async `ToolRegistry` / `Runner`** and **metaclass auto-registration of tools** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_tools.py`](tests/test_tools.py).

- **Enterprise integrator:** **`REPLAYT_RUN_STARTED_TS`** — trusted policy hooks (**`run_hook`**, **`resume_hook`**, **`export_hook`**, **`seal_hook`**, **`verify_seal_hook`**) receive the ISO 8601 UTC envelope **`ts`** from the first **`run_started`** line when it is a non-empty string, so org gates can correlate exports and approvals with the original run start time without **`jq`**. **`run_hook`** sets it only when **`replayt run` / `replayt ci`** uses **`--resume`** and the JSONL already contains **`run_started`**. **`policy_hook_env_catalog`** lists the name on all five hooks. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt try --print-snippet KEY`** prints a single copy-paste shell line for the packaged **`--example`** (try / copy / **`MODULE:VAR`** / **`replayt run … --inputs-json …`** / **`run_dry_check`**) and exits without running the workflow; **`--output json`** emits **`replayt.try_print_snippet.v1`**. Implemented in [`src/replayt_examples/catalog.py`](src/replayt_examples/catalog.py) and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); **`cli_json_stdout_contract`** / **`cli_machine_readable_schemas`** in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`docs/CLI.md`](docs/CLI.md); compose-outside **`replayt deps` / `pip-freeze` shims** and **vault-driven `.env` sync** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: dependency lockfiles and secret sync** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** **`python scripts/example_catalog_contract.py --format json`** (**`replayt.example_catalog_contract.v1`**) now includes **`example_sha256s`**: a parallel list of SHA-256 hex digests (UTF-8, one per catalog row in **`examples`** order) from canonical JSON per row, so fork CI can detect which packaged **`replayt try`** entry drifted without comparing the entire contract file. Implemented in [`scripts/example_catalog_contract.py`](scripts/example_catalog_contract.py); documented in [`CONTRIBUTING.md`](CONTRIBUTING.md); checked-in [`docs/EXAMPLE_CATALOG_CONTRACT.json`](docs/EXAMPLE_CATALOG_CONTRACT.json) regenerated; regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).

- **Product engineer:** **`replayt diff RUN_A RUN_B`** accepts **`--style stakeholder`** or **`support`** for **text** output: after the usual side-by-side summary, replayt prints the same **Stakeholder CLI handoff** Markdown section as **`replayt report-diff`** (regenerate comparison, JSON diff, per-run **`inspect`** / **`report`** / **`replay`**, **`bundle-export`**, JSON **`inspect`** on failed sides, **`replayt resume`** hints when paused), honoring repeatable **`--llm-model`** in the copy-paste lines. **`--output json`** ignores **`--style`**. Implemented in [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Two-run comparison** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt.ci_run_summary.v1`** (from **`replayt ci --summary-json`**, **`replayt run`** with **`REPLAYT_SUMMARY_JSON`**, or env **`REPLAYT_SUMMARY_JSON`**) now includes **`ci_marker_env`**: for each name in a stable vendor list (**`CI`**, **`GITHUB_ACTIONS`**, **`GITLAB_CI`**, …), a boolean records whether that variable is set and non-empty (**values are never copied**). **`host_clock_utc_offset_minutes`** records the local wall clock's offset from UTC in whole minutes when **`datetime.now().astimezone()`** succeeds, else **`null`**, so archived summaries help classify which runner flavor hosted the job and spot timezone skew without coupling to a single CI vendor's env names. **`CI_MARKER_ENV_NAMES`** is exported from [`src/replayt/cli/ci_artifacts.py`](src/replayt/cli/ci_artifacts.py). Documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`TMPDIR`/`TEMP` probes on `operational_paths.v1`** and **`umask` on `ci_run_summary.v1`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: temp directory probes and `umask` snapshots** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** Optional **`llm_tags`** (sorted unique short strings) on **`LLMBridge`**: set via workflow **`llm_defaults`**, **`ctx.llm.with_settings(llm_tags=...)`** (unioned with prior tags), or per-call **`complete_text(..., llm_tags=...)`** / **`parse(..., llm_tags=...)`**. Tags are logged on **`effective`** and mirrored top-level on **`llm_request`**, **`llm_response`**, **`structured_output`**, and **`structured_output_failed`**; they are audit-only on the HTTP request (**`extra_body`** cannot use **`llm_tags`** as a key). Implemented in [`src/replayt/llm.py`](src/replayt/llm.py) and [`src/replayt/llm_coercion.py`](src/replayt/llm_coercion.py); documented in [`README.md`](README.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/RECIPES.md`](docs/RECIPES.md), [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py) and [`tests/test_llm_coercion.py`](tests/test_llm_coercion.py).

- **Security / compliance:** On **POSIX** hosts, **new** per-run **`*.jsonl`** files created by **`JSONLStore`** are **`chmod`**’d to **owner read/write only (`0600`)** after the store seeds the empty file, so fresh audit logs are not group- or world-readable by default. **`REPLAYT_JSONL_POSIX_MODE`** (**`inherit`** / octal override) is read when the CLI builds the primary store via **`make_store`**; **`JSONLStore(..., posix_new_file_mode=None)`** skips **`chmod`** for **`Runner.run`** call sites that need umask-only semantics. Implemented in [`src/replayt/persistence/jsonl.py`](src/replayt/persistence/jsonl.py) and [`src/replayt/cli/stores.py`](src/replayt/cli/stores.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **built-in policy-hook stdout/stderr byte caps** and **doctor hard-fail on permissive existing JSONL** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_jsonl_store.py`](tests/test_jsonl_store.py).

- **Junior onboarding:** **`replayt validate`**, **`replayt graph`**, and **`replayt contract`** now resolve the workflow **`TARGET`** the same way as **`replayt run`** / **`replayt ci`**: optional positional when **`REPLAYT_TARGET`** or project **`target`** is set (CLI arg still wins). **`cli_run_defaults_contract`** / **`project_setting_precedence_contract`** text on **`replayt version --format json`** and docs (**`docs/CLI.md`**, **`docs/CONFIG.md`**, tutorial **`src/replayt_examples/README.md`**) describe the expanded command list; regressions in **`tests/test_cli.py`**.

- **Staff / platform:** **`replayt version --format json`** now includes **`run_result_status_contract`** (`replayt.run_result_status_contract.v1`): the only **`status`** strings on **`replayt.run_result.v1`** (**`completed`**, **`failed`**, **`paused`**), each with **`typical_exit_code`** and **`workflow_subcommands`** aligned to **`cli_exit_codes.workflow_run`**, plus cross-references to the payload **`schema_key`**. Registered under **`cli_machine_readable_schemas.run_result_status_contract`**. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform (archetype brainstorm):** logged compose-outside rejections for a global **`replayt.state_names_contract.v1`** on **`replayt version --format json`** and for a bundled **`replayt diagnostics --format json`** in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); per-workflow graphs stay on **`replayt contract`**, and multi-command JSON merges stay in **your** pipeline (**Pattern: workflow contract allowlist** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); **Beyond core: merging `replayt version` and `replayt config` JSON** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md)).

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.22`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.22 - 2026-03-23

- **Reviewcodebase pass:** **`OpenAICompatClient.chat_completions`** retry backoff now parses **`Retry-After`** through **`_retry_after_delay_seconds`**: negative, non-finite, and empty numeric values fall back to **`_RETRY_BASE_DELAY`** before applying exponential backoff and the max delay cap, so **`time.sleep`** never receives invalid durations from a hostile or buggy gateway. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).

- **Docs:** deslopdoc pass on [`README.md`](README.md), [`docs/COMPARISON.md`](docs/COMPARISON.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md): drop duplicate LangGraph "use the other tool" framing, replace "audit story" / "instant browsable" phrasing, split the long per-call LLM settings sentence in scope, tighten the archetype intro, and swap "you should be able to" for a direct "you can".

- **Improvedoc pass:** **`coerce_top_p`** and **`coerce_openai_penalty`** now reject non-finite floats (for example **`nan`** and **`inf`**). Range checks alone did not catch those values because comparisons with **`NaN`** are false in Python, which could otherwise produce invalid HTTP payloads for OpenAI-compatible **`top_p`** / penalty fields. Implemented in [`src/replayt/llm_coercion.py`](src/replayt/llm_coercion.py); regressions in [`tests/test_llm_coercion.py`](tests/test_llm_coercion.py).

- **Agent harness / automation:** **`SKILL_RUN_STAMP`** (run directory basename, often **`YYYYMMDD-HHMMSS`**) is injected on every **`--skill-command`** subprocess, exposed as **`{run_stamp}`** / **`{run_stamp_q}`** in templates, recorded on each **`replayt.skill_invocation.v1`** sidecar as **`run_stamp`**, and written to **`pipeline.json`** (**`replayt.skill_release_pipeline.v1`**) so harnesses can tag CI artifacts without parsing full paths. **`skill_loop_env_contract`** and **`skill_loop_placeholder_contract`** in **`replayt version --format json`** list the new name. Implemented in [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py), [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py), [`src/replayt/cli/skill_loop_env.py`](src/replayt/cli/skill_loop_env.py), and [`src/replayt/cli/skill_loop_placeholders.py`](src/replayt/cli/skill_loop_placeholders.py); documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **built-in `latest` symlink** and **NDJSON stderr progress per skill** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py), and [`tests/test_skill_loop_placeholders.py`](tests/test_skill_loop_placeholders.py).

- **MCP / tooling:** **`cli_json_stdout_contract`** rows (under **`subcommands`**) now include **`schema`**: the full **`replayt.*.v1`** payload id for that argv shape, matching **`cli_machine_readable_schemas[schema_key]`** so subprocess and MCP hosts can type stdout without a second lookup. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **per-route stdin-hazard booleans on every JSON route** and **embedded `subprocess.run` argv templates in version JSON** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`ToolRegistry.anthropic_messages_tools()`** builds sorted Anthropic Messages API **`tools=`** payloads (**`name`**, **`input_schema`**, optional **`description`**) from registered handlers, using the same JSON Schema derivation as **`openai_chat_tools()`**. **`apply_anthropic_tool_use_blocks(...)`** walks assistant **`content`** (or a list of blocks), skips non-**`tool_use`** entries, and runs each **`tool_use`** through **`call`** in list order so JSONL **`tool_call`** / **`tool_result`** events match the OpenAI composition path. Implemented in [`src/replayt/tools.py`](src/replayt/tools.py); documented in [`README.md`](README.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **LangGraph (and similar frameworks)** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`ToolRegistry` Gemini `FunctionDeclaration` builders** and **runner-level automatic multi-turn tool loops** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_tools.py`](tests/test_tools.py).

- **Enterprise integrator:** **`resume_hook`**, **`export_hook`**, **`seal_hook`**, and **`verify_seal_hook`** now receive optional **`REPLAYT_RUN_INPUTS_JSON`**: canonical sorted JSON of **`run_started.inputs`** from the run log (or **`{}`** when **`inputs`** is absent or null), matching the **`run_hook`** contract so policy scripts can gate approvals, exports, and seals on the same input payload without **`jq`**. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py), and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); **`policy_hook_env_catalog`** lists the name on those four hooks; documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt validate TARGET --print-inputs-template`** (text mode only) prints a compact JSON object on stdout after a successful graph check: sorted union of every **`@wf.step(expects=...)`** key with type-shaped placeholders, and **`null`** when the same key uses conflicting types on different steps. Mutually exclusive with **`--format json`**; stderr notes how to save or pipe the blob. Implemented in [`src/replayt/cli/validation.py`](src/replayt/cli/validation.py) (`workflow_inputs_template`) and [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: desktop notifications and detached runs** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **desktop completion toasts** and **`replayt run` as a background daemon** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** **`python scripts/pyproject_pep621_report.py --format json`** (**`replayt.pyproject_pep621_report.v1`**) now includes normalized **`[project]`** package metadata alongside dependency pins: **`description`**, **`readme_file`** / **`readme_content_type`** / **`readme_text_sha256`** (inline readme text is fingerprinted, not echoed), SPDX **`license_expression`** or **`license_file`** / **`license_text_sha256`**, sorted **`keywords`** and **`classifiers`**, sorted **`urls`**, and stable sorted **`authors`** / **`maintainers`** rows (**`name`**, **`email`**). Fork CI can diff trove classifiers, homepage URLs, and readme paths without hand-parsing TOML. Implemented in [`scripts/pyproject_pep621_report.py`](scripts/pyproject_pep621_report.py); documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Forks and release hygiene** in [`README.md`](README.md); compose-outside **`replayt pyproject-validate`** / bundled **`validate-pyproject`** in **`maintainer_checks.py`** and **checked-in PyPI long-description generators** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).

- **Product engineer:** **`replayt replay --format html`** with **`--style stakeholder`** or **`support`** now embeds the same **Stakeholder CLI handoff** section as **`replayt report`** (inspect, report regen, bundle-export, resume when paused, JSON inspect when failed), with matching inline CSS, so PMs and support get copy-paste next steps from offline timeline pages and **`bundle-export`** **`timeline.html`** without relying only on **`report.html`**. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: public pastebins and bundled approval UIs** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside text-replay handoff and **`runs --format parquet`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_display.py`](tests/test_display.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt.ci_run_summary.v1`** (from **`replayt ci --summary-json`**, **`replayt run`** with **`REPLAYT_SUMMARY_JSON`**, or env **`REPLAYT_SUMMARY_JSON`**) now includes **`filesystem_encoding`** (**`sys.getfilesystemencoding()`**), **`stdout_encoding`** (**`sys.stdout.encoding`** when present), and on POSIX **`ulimit_nofile_soft`** / **`ulimit_nofile_hard`** from **`resource.getrlimit(RLIMIT_NOFILE)`** when the call succeeds (otherwise **`null`**, including on Windows), so archived summaries help triage Unicode path issues and **`EMFILE`**-style failures without re-parsing CI logs. Implemented in [`src/replayt/cli/ci_artifacts.py`](src/replayt/cli/ci_artifacts.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), **Beyond core: explicit CI artifact paths** / **disk quotas** / **auto-filled `ci_metadata`** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`sys.argv` echo in `ci_run_summary.v1`** and **`replayt doctor` cgroup memory limit probes** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** `ctx.llm.with_settings(http_retries=...)`, workflow `llm_defaults["http_retries"]`, and per-call `complete_text(..., http_retries=...)` / `parse(..., http_retries=...)` override how many *extra* POST attempts the OpenAI-compat client makes after retryable HTTP statuses (429, 500, 502, 503, 504). The resolved value is always logged on `effective` (and included in `effective_sha256`). Values are coerced with the same rules as `LLMSettings.http_retries`, capped at 25. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py) and [`src/replayt/llm_coercion.py`](src/replayt/llm_coercion.py); [`src/replayt/testing.py`](src/replayt/testing.py) mock clients accept the new `chat_completions` kwarg; documented in [`README.md`](README.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/RECIPES.md`](docs/RECIPES.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py), [`tests/test_llm_coercion.py`](tests/test_llm_coercion.py), and [`tests/test_runner.py`](tests/test_runner.py).

- **Security / compliance:** trusted policy hooks (**`run_hook`**, **`resume_hook`**, **`export_hook`**, **`seal_hook`**, **`verify_seal_hook`**) now receive **`REPLAYT_POLICY_HOOK_NAME`**, set to the stable catalog id for that invocation (`run_hook`, `resume_hook`, `export_hook`, `seal_hook`, or `verify_seal_hook`), so shared gate scripts and downstream audit parsers can tell which gate fired without re-tokenizing argv. **`policy_hook_env_catalog`** in **`replayt version --format json`** lists the name on every hook. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Junior onboarding:** when **`replayt run`** / **`replayt ci`** have **no** resolved target (no positional **`TARGET`**, no **`REPLAYT_TARGET`**, no project **`target`**), the Typer error now appends **copy-paste examples** (**`replayt run replayt_examples.e01_hello_world:wf …`**, **`replayt try --list`**, **`replayt init`**) plus doc pointers so a blank first invocation surfaces a next step immediately. Implemented in [`src/replayt/cli/config.py`](src/replayt/cli/config.py); documented in [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Junior onboarding (archetype brainstorm):** logged compose-outside rejections for a **silent default packaged hello-world target** and for **directory-as-TARGET auto-discovery** in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); **Common `TARGET` mistakes** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md) now states directory paths are not scanned and bare **`replayt run`** does not pick an example workflow implicitly.

- **Staff / platform:** **`replayt version --format json`** now includes **`project_setting_precedence_contract`** (`replayt.project_setting_precedence_contract.v1`): cwd-independent ordered precedence for merged CLI / env / project settings beyond optional **`TARGET`** and **`resolve_run_inputs_json`** (default **`log_dir`**, **`log_mode`**, **`forbid_log_mode_full`**, redaction and approval policy fields, **`sqlite`**, **`strict_mirror`**, subprocess **`--timeout`**, LLM defaults, **`policy_hook_context_json`**, and trusted policy hook argv plus hook timeouts with **replace-not-merge** semantics for env vs TOML). **`extends_cli_run_defaults_contract`** points at **`replayt.cli_run_defaults_contract.v1`** so workflow target and inputs stay single-sourced. Implemented in [`src/replayt/cli/config.py`](src/replayt/cli/config.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); registered under **`cli_machine_readable_schemas.project_setting_precedence_contract`**; documented in [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/CLI.md`](docs/CLI.md), and **Beyond core: merging `replayt version` and `replayt config` JSON** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform (archetype brainstorm):** logged compose-outside rejections for a **`--config-file`** / forced rc path flag and for auto **`replayt contract --write`** after every successful run in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); **Beyond core: cwd for config discovery and contract check jobs** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md) documents **`cd`** / CI **`working-directory`**, **`project_config_resolution`**, and **`replayt contract --check`** instead.

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.21`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.21 - 2026-03-23

- **Reviewcodebase pass:** **`replayt report`**, **`replayt report-diff`**, **`replayt export-run`**, and **`replayt bundle-export`** validate **`run_id`** up front (same rules as JSONL basenames) and exit **1** with the standard message instead of raising **`ValueError`** from the store layer. Tar prefixes and manifest **`run_id`** use the validated value. Implemented in [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Docs:** deslopdoc pass on [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/CLI.md`](docs/CLI.md), [`README.md`](README.md), [`src/replayt_examples/README.md`](src/replayt_examples/README.md), and [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md): replace em-dash chains with periods, parentheses, or commas where a shorter break reads more naturally; rename the archetype brainstorm closing heading to **Reference table**.

- **Improvedoc pass:** **`fix_check`** / **`fix_pre_tag_ci`** rounds now pass **`{task}`** into **`--skill-command`** template expansion in **`scripts/skill_release_loop.py`** and **`scripts/skill_release_loop_agent.py`**, matching **`SKILL_TASK`** and **`replayt version --format json`** → **`skill_loop_placeholder_contract`**. Fix-task strings are module constants **`FIX_CHECK_TASK`** / **`FIX_PRE_TAG_CI_TASK`**. **`ToolRegistry.apply_openai_chat_tool_calls`** wraps invalid JSON in string **`function.arguments`** as **`ValueError`** with the tool-call index. Implemented in [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py), [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py), and [`src/replayt/tools.py`](src/replayt/tools.py); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py), and [`tests/test_tools.py`](tests/test_tools.py).

- **Agent harness / automation:** **`replayt version --format json`** now includes **`skill_loop_placeholder_contract`** (`replayt.skill_loop_placeholder_contract.v1`): sorted **`skill_command_placeholders`** and **`check_command_placeholders`** rows (**`name`**, **`description`**) documenting every **`str.format_map`** key for **`scripts/skill_release_loop*.py`** **`--skill-command`** and **`--check`** templates (plus **`notes`** on **`{name}_q`** shell-quoted variants). Documents **`{task}`** explicitly: full shared **`--task`** for normal skills, short fix instructions for **`fix_check`** / **`fix_pre_tag_ci`**, while **`{task_sha256}`** always fingerprints the outer task. **`--help`** epilogs and **Automated skill-loop release** in [`CONTRIBUTING.md`](CONTRIBUTING.md) now list **`{task}`**. Implemented in [`src/replayt/cli/skill_loop_placeholders.py`](src/replayt/cli/skill_loop_placeholders.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`docs/CLI.md`](docs/CLI.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_skill_loop_placeholders.py`](tests/test_skill_loop_placeholders.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **MCP / tooling:** **`replayt version --format json`** → **`cli_json_stdout_contract`** now includes **`trust_profile_machine_flags`**: each **`trust_profile`** id maps to stable booleans (**`appends_run_timeline`**, **`executes_workflow_runner`**, **`may_invoke_trusted_policy_hooks`**, **`writes_seal_manifest`**, **`writes_scaffold_paths`**, **`may_probe_default_llm_http`**) so subprocess and MCP hosts can classify JSON-on-stdout routes without parsing prose **`trust_profiles`**. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt codegen-mcp-stubs`** (bundled Typer→MCP tool generator) and **env-forced JSON stderr for all Typer usage errors** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`ToolRegistry.apply_openai_chat_tool_calls(...)`** accepts OpenAI Chat Completions-style **`tool_calls`** dicts (``type: function``, nested ``function.name`` / ``function.arguments`` as JSON string or object), decodes arguments, and invokes **`call`** in list order so vendor-SDK **`tool_calls`** round-trips stay aligned with **`openai_chat_tools()`** and JSONL **`tool_call`** / **`tool_result`** events. Implemented in [`src/replayt/tools.py`](src/replayt/tools.py); documented in [`README.md`](README.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **LangGraph (and similar frameworks)** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_tools.py`](tests/test_tools.py).

- **Enterprise integrator:** **`REPLAYT_POLICY_HOOK_CONTEXT_JSON`** — optional hook-only JSON (not written to JSONL) forwarded to every trusted policy subprocess when set via **`--policy-hook-context-json`** on **`replayt run`**, **`ci`**, **`resume`**, **`export-run`**, **`bundle-export`**, **`seal`**, or **`verify-seal`** (supports **`@path`** / **`@-`** like **`--inputs-json`**), env **`REPLAYT_POLICY_HOOK_CONTEXT_JSON`**, or **`[tool.replayt] policy_hook_context_json`**, with precedence **CLI > env > project config**. Canonical sorted JSON is injected on each hook invocation; **`policy_hook_env_catalog`** lists the name on all five hooks. **`replayt run --dry-check`** validates **`--policy-hook-context-json`** via **`replayt.validate_report.v1`**. Implemented in [`src/replayt/cli/config.py`](src/replayt/cli/config.py), [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/cli/validation.py`](src/replayt/cli/validation.py), [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt init-env-example`** (**`--path`**, default **`.`**; **`--force`** to overwrite) writes the same **`.env.example`** template as **`replayt init`**, including commented **`REPLAYT_TARGET`** and **`REPLAYT_INPUTS_FILE`** hints, without other scaffold files. **`INIT_ENV_EXAMPLE`** in [`src/replayt/cli/constants.py`](src/replayt/cli/constants.py) is the single source of truth for both commands. Implemented in [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); documented in [`docs/CLI.md`](docs/CLI.md) and the tutorial paragraph beside **`replayt init-gitignore`** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py). Compose-outside **`replayt init --pytest`** (bundled golden-path test scaffold) and **`replayt snippets`** (CLI that dumps Runner boilerplate) logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **## Tests without a live LLM (CI and pytest)** / **greenfielding** guidance in [`src/replayt_examples/README.md`](src/replayt_examples/README.md) and **Pattern: golden path test** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md).

- **Open-source maintainer:** **`python scripts/changelog_unreleased.py --format json`** (**`replayt.unreleased_changelog.v1`**) now includes **`item_sha256s`**: a parallel list of SHA-256 hex digests (UTF-8, one per parsed top-level bullet) so fork CI can assert a specific Unreleased entry changed or stayed stable without diffing the full section body. Implemented in [`scripts/changelog_unreleased.py`](scripts/changelog_unreleased.py); documented in [`CONTRIBUTING.md`](CONTRIBUTING.md), **Forks and release hygiene** in [`README.md`](README.md), and **Beyond core: Unreleased text on `replayt version` JSON and changelog auto-formatters** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt version` JSON embedding Unreleased markdown from cwd** and **bundled changelog line-wrapping scripts** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).

- **Product engineer:** **Stakeholder CLI handoff** (HTML/Markdown) on **`replayt report`**, **`replayt report-diff`**, and **`inspect --output markdown`** now includes copy-paste **`replayt bundle-export … --out <run_id>-stakeholder-bundle.tar.gz --report-style …`** lines so PMs and support can rebuild the same stakeholder tarball (report + timeline + sanitized JSONL + manifest) without hunting **`bundle-export`** docs. **`--report-style`** matches **`stakeholder`** vs **`support`** for the current document. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py) and [`src/replayt/cli/report_template.py`](src/replayt/cli/report_template.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Two-run comparison** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_display.py`](tests/test_display.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt.ci_run_summary.v1`** (from **`replayt ci --summary-json`**, **`replayt run`** with **`REPLAYT_SUMMARY_JSON`**, or env **`REPLAYT_SUMMARY_JSON`**) now includes **`python_implementation`** (**`platform.python_implementation()`**, for example **CPython** vs **PyPy**) and **`machine`** (**`platform.machine()`**, CPU architecture) next to the existing interpreter stamps, so archived summaries distinguish mixed-matrix jobs without opening the full CI log. Implemented in [`src/replayt/cli/ci_artifacts.py`](src/replayt/cli/ci_artifacts.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt ci --retry` / built-in flake retries with backoff** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Pattern: batch driver** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** **`ctx.llm.with_settings(response_format={...})`** now merges an OpenAI-style **`response_format`** into bridge defaults and forwards it on **`ctx.llm.complete_text`** (use **`complete_text(..., response_format=None)`** to omit for one call; **`with_settings(response_format={})`** clears defaults). **`llm_request`** / **`llm_response`** / structured-output **`effective`** gain a compact **`response_format`** breadcrumb (**`type`**, optional **`json_schema_name`** / **`json_schema_strict`**) whenever the HTTP request includes **`response_format`**, including native **`parse`** with **`native_response_format=True`**, without logging the full embedded JSON Schema. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); documented in [`README.md`](README.md), [`docs/RECIPES.md`](docs/RECIPES.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), and **OpenAI `response_format` on `complete_text`** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt inspect` / `runs` `--response-format-type`** and **automatic continuation when `finish_reason` is `length`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).

- **Security / compliance:** expanded **`credential_env`** / **`llm.credential_env`** presence-only audit in **`replayt doctor --format json`** and **`replayt config --format json`** with additional common gateway API key names (**`DEEPSEEK_API_KEY`**, **`FIREWORKS_API_KEY`**, **`GOOGLE_API_KEY`**, **`OLLAMA_API_KEY`**, **`PERPLEXITY_API_KEY`**, **`TOGETHER_API_KEY`**, **`XAI_API_KEY`**); the canonical tuple is sorted alphabetically. Implemented in [`src/replayt/security.py`](src/replayt/security.py); documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_security.py`](tests/test_security.py).

- **Junior onboarding:** after a passing **`replayt validate TARGET`** in **text** mode, the CLI prints **Next:** (**`replayt run TARGET … --dry-check`**, repeating **`--strict-graph`** when you used it on validate) and **Then:** (**`replayt run TARGET …`** with inputs) so the first successful graph check flows straight into copy-paste run commands. **`--format json`** is unchanged (machine-only stdout). Implemented in [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md); compose-outside **`replayt install-extra` / pip-from-CLI** and **`replayt doctor --create-venv`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: bundled pip extra installers and venv scaffolding** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** **`replayt version --format json`** now includes **`cli_run_defaults_contract`** (`replayt.cli_run_defaults_contract.v1`): cwd-independent ordered precedence for optional **`TARGET`** on **`run` / `ci`** and for **`resolve_run_inputs_json`** on **`run` / `ci` / `validate` / `doctor --target`**, plus a note that other subcommands require an explicit **`TARGET`** on argv. Implemented in [`src/replayt/cli/config.py`](src/replayt/cli/config.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md); registered under **`cli_machine_readable_schemas.cli_run_defaults_contract`**; regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.20`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.20 - 2026-03-23

- **Reviewcodebase pass:** **`--run-id`** is validated with the same rules as JSONL basenames **before** **`run_hook`** / **`resume_hook`** build **`REPLAYT_RUN_JSONL`**, so path segments such as **`..`** cannot make hook env point outside the resolved log directory. **`invoke_run_hook`** and **`invoke_resume_hook`** call **`validate_run_id`** as a second line of defense for programmatic callers. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); regression in [`tests/test_cli.py`](tests/test_cli.py).

- **Agent harness / automation:** **`replayt version --format json`** now includes **`skill_loop_env_contract`** (`replayt.skill_loop_env_contract.v1`): **`main_injected_env`** and **`fix_injected_env`** arrays of **`name`** / **`description`** for every variable **`scripts/skill_release_loop*.py`** sets on **`--skill-command`** subprocesses (excluding optional `GIT_CONFIG_*` injections). The canonical key lists live in [`src/replayt/cli/skill_loop_env.py`](src/replayt/cli/skill_loop_env.py) and are imported by the loop scripts so sidecar **`injected_env_keys`** and the version report stay aligned. Registered under **`cli_machine_readable_schemas.skill_loop_env_contract`**; documented in [`docs/CLI.md`](docs/CLI.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **MCP / tooling:** **`replayt version --format json`** → **`cli_json_stdout_contract`** now includes **`typer_pre_dispatch_phase`**, documenting that Typer usage failures (bad argv before dispatch) typically exit **2** with stderr-heavy output—overlapping exit **2** for approval pauses on **`run` / `ci` / `resume` / `try`**. **`cli_exit_codes`** adds sibling **`typer_pre_dispatch_failures`** with disambiguation: on **`run` / `ci` / `try`** with **`--output json`**, parseable **`replayt.run_result.v1`** with **`status`** **`paused`** means a real pause versus non-JSON stdout after Typer errors; **`resume`** adds **`disambiguation_for_resume_text_stdout`** (no JSON route). Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`ToolRegistry.openai_chat_tools()`** builds sorted OpenAI Chat Completions **`tools=`** payloads from registered handlers (Pydantic **`TypeAdapter`** JSON Schema per parameter, first docstring paragraph as **`function.description`**). Use it inside a vendor-SDK **`@wf.step`**, then run each model-chosen call through **`ctx.tools.call`** so **`tool_call`** / **`tool_result`** JSONL stays consistent. Implemented in [`src/replayt/tools.py`](src/replayt/tools.py); documented in [`README.md`](README.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **LangGraph (and similar frameworks)** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt graph --format json`** (duplicate of **`contract --format json`**) and **first-class SSE streaming on `OpenAICompatClient` / `LLMBridge`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_tools.py`](tests/test_tools.py).

- **Enterprise integrator:** trusted policy hooks (**`run_hook`**, **`resume_hook`**, **`export_hook`**, **`seal_hook`**, **`verify_seal_hook`**) now receive optional **`REPLAYT_WORKFLOW_META_JSON`**: sorted JSON of **`run_started.workflow_meta`** (that is, **`Workflow.meta`** with **`llm_defaults`** removed—the same rule the runner uses when emitting **`run_started`**). **`run_hook`** gets it from the loaded workflow before the first JSONL line; other hooks read it from the run's first **`run_started`**. **`policy_hook_env_catalog`** lists the name on every hook. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt init-gitignore`** (**`--path`**, default **`.`**) merges the same **`.replayt/`**, **`.env`**, **`.venv/`**, and **`__pycache__/`** lines as **`replayt init`** without writing scaffold files—handy after **`replayt try --copy-to`** or a hand-rolled workflow. Implemented in [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); documented in [`docs/CLI.md`](docs/CLI.md), the tutorial paragraph after **`replayt try --copy-to`**, and **Beyond core: public pastebins and bundled approval UIs** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt paste-run` / public paste upload of JSONL** and **bundled FastAPI approval UI in the wheel** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** **`python scripts/changelog_unreleased.py --format json`** (**`replayt.unreleased_changelog.v1`**) now includes **`body_sha256`**: SHA-256 hex of the UTF-8 **`## Unreleased`** body so fork CI can cache or gate on whether that section changed. Documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Forks and release hygiene** in [`README.md`](README.md); compose-outside **parent-directory `CHANGELOG.md` discovery** and **bundled post-tag announcement / social release scripts** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).

- **Product engineer:** **`replayt report-diff`** with **`--style stakeholder`** or **`support`** (HTML and Markdown) now ends with the same **Stakeholder CLI handoff** section pattern as **`replayt report`**, extended for two runs: regenerate the comparison, **`replayt diff … --output json`**, per-run **`replayt inspect`** / stakeholder **`replayt report`** / offline **`replayt replay`**, JSON **`inspect`** when a side failed, and labeled **`replayt resume`** hints for each paused run. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py), [`src/replayt/cli/report_template.py`](src/replayt/cli/report_template.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Two-run comparison** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt.ci_run_summary.v1`** (from **`replayt ci --summary-json`**, **`replayt run`** with **`REPLAYT_SUMMARY_JSON`**, or env **`REPLAYT_SUMMARY_JSON`**) now includes **`cwd`** (resolved working directory when the summary is written) and **`python_executable`** next to **`python_version`** and **`platform`**, so archived CI artifacts distinguish monorepo checkout roots and accidental wrong-venv interpreters without re-parsing logs. Implemented in [`src/replayt/cli/ci_artifacts.py`](src/replayt/cli/ci_artifacts.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **multi-testcase JUnit from `replayt ci`** and **bundled `script(1)` / TTY transcript wrapper in core** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: richer JUnit matrices and shell transcripts** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** optional **`call_label`** on **`ctx.llm.with_settings(call_label=...)`** (whitespace-stripped, max 128 characters) tags a single LLM round trip when one step issues several **`parse`** / **`complete_text`** calls. The value is logged under **`effective`**, duplicated as a top-level field on **`llm_request`**, **`llm_response`**, **`structured_output`**, and **`structured_output_failed`** when set, and cleared by **`with_settings(call_label="")`**. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); documented in [`README.md`](README.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/RECIPES.md`](docs/RECIPES.md), [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), and **Multiple LLM calls in one handler (`call_label`)** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **first-class embeddings on `LLMBridge`** and **typed `parallel_tool_calls` separate from `extra_body`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).

- **Security / compliance:** **`run_hook`**, **`resume_hook`**, and **`export_hook`** (when **`--target`** is set) subprocesses now receive optional **`REPLAYT_WORKFLOW_ENTRY_PATH`**: the absolute path to the workflow **`.py`** / YAML file or the imported module's **`__file__`**, when replayt can resolve **`REPLAYT_TARGET`** (same rules as **`replayt doctor`** workflow entry trust checks). **`policy_hook_env_catalog`** lists the name on those three hooks. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Junior onboarding:** when **`replayt run`** / **`replayt ci`** **`TARGET`** is a filesystem path to an existing file that is not **`.py`** / **`.yaml` / `.yml`** (a common mistake is passing **`inputs.json`** first), the error now separates workflow entrypoints from inputs: copy-paste **`--inputs-file`** hints for **`.json`**, config-vs-workflow guidance for **`.toml`**, and doc pointers for text-like extensions. Documented in [`docs/CLI.md`](docs/CLI.md) and **Common `TARGET` mistakes** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt hello`-style magic packaged runs** and **auto-treating a lone `.json` path as implicit `--inputs-file`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** **`replayt version --format json`** now includes **`project_config_resolution`** (`replayt.project_config_resolution.v1`): cwd, ordered **`walk`** rows (**`replaytrc_toml`**, **`pyproject_toml`**, **`pyproject_has_tool_replayt`**, **`stopped_here`**), the winning config **`winner`** (**`path`**, **`kind`**: **`replaytrc`** / **`pyproject`**), plus **`unknown_keys`** and **`shadowed_sources`** matching **`replayt config`**. Config loading records the walk in the same pass as **`get_project_config()`**. Documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Improvedoc pass:** **`ToolRegistry.openai_chat_tools()`** raises **`ValueError`** when a registered callable's **`__name__`** is not a valid OpenAI Chat Completions **`function.name`** (1-64 ASCII letters, digits, underscore, or hyphen), so invalid identifiers fail locally instead of at the gateway. Added [`tests/test_skill_loop_env.py`](tests/test_skill_loop_env.py) to assert **`fix_injected_env`** keys stay a subset of **`main_injected_env`**, that fix steps omit **`SKILL_PATH`** / **`SKILL_REQUESTED_NAME`**, and that **`build_skill_loop_env_contract()`** rows match the canonical tuples with non-empty descriptions. Implemented in [`src/replayt/tools.py`](src/replayt/tools.py); regressions in [`tests/test_tools.py`](tests/test_tools.py).

- **Docs:** deslopdoc pass on [`docs/QUICKSTART.md`](docs/QUICKSTART.md), [`docs/PRODUCTION.md`](docs/PRODUCTION.md), [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), [`src/replayt_examples/README.md`](src/replayt_examples/README.md), and [`src/replayt/notebook.py`](src/replayt/notebook.py): replay HTML is self-contained embedded CSS (not Tailwind via CDN); shorter sentences in the tutorial and review note; comment punctuation in the notebook module.

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.19`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.19 - 2026-03-23

- **Reviewcodebase pass:** JSONL **`type`** values are only used in **`x in frozenset`** / **`x in {...}`** checks after **`jsonl_type_str(...)`** (must be a real **`str`**), so hand-edited or hostile logs with list/dict **`type`** fields no longer raise **`TypeError`** in **`replay_timeline_lines`**, **`run_matches_structured_schema_name_filter`**, **`run_matches_llm_model_filter`**, or inspect event filters. Missing **`type`** renders as **`unknown`** in replay timeline text. **`mermaid_label`** coerces with **`str(...)`** before **`html.escape`**. Jupyter **`display_run`** uses string **`type`** labels for badges/details and only applies the state row highlight when **`type`** is the string **`state_entered`**. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py), [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py), [`src/replayt/graph_export.py`](src/replayt/graph_export.py), and [`src/replayt/notebook.py`](src/replayt/notebook.py); regressions in [`tests/test_display.py`](tests/test_display.py), [`tests/test_graph_export.py`](tests/test_graph_export.py), and [`tests/test_notebook.py`](tests/test_notebook.py).

- **Docs:** deslopdoc pass on [`docs/CLI.md`](docs/CLI.md) (policy-hook env sentence), [`docs/CONFIG.md`](docs/CONFIG.md) (config resolution and `replayt config` wording), [`docs/RECIPES.md`](docs/RECIPES.md) and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md) (split em-dash chains), [`src/replayt_examples/README.md`](src/replayt_examples/README.md) (same), and maintainer header comments in [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py) / [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py) (plainer feat-skill / review_design_fidelity notes).

- **Improvedoc pass:** **`load_workflow_yaml`** wraps **`yaml.safe_load`** failures in **`ValueError`** with **`Invalid YAML in workflow file <path>`** so broken workflow files surface the file path instead of a bare PyYAML scanner/parser error. Implemented in [`src/replayt/yaml_workflow.py`](src/replayt/yaml_workflow.py); regression in [`tests/test_yaml_workflow.py`](tests/test_yaml_workflow.py).

- **Design fidelity:** **`replayt` package root** now imports **`display_graph`** and **`display_run`** from **`replayt.notebook`** unconditionally so **`__all__`** never advertises names that were skipped by a silent **`ImportError`** (the notebook module already degrades gracefully when IPython is absent). Regression in [`tests/test_version_metadata.py`](tests/test_version_metadata.py).

- **Agent harness / automation:** **`scripts/skill_release_loop.py`** and **`scripts/skill_release_loop_agent.py`** add **`skill_loop_task_sha256`**: UTF-8 SHA-256 of the shared **`--task`** string as **`task_sha256`** in **`pipeline.json`** and each **`replayt.skill_invocation.v1`** sidecar, plus **`SKILL_TASK_SHA256`** and **`{task_sha256}`** for **`--skill-command`** templates (fix prompts reuse the outer loop digest while the sidecar **`task`** text stays the short fix instruction). **`--resume`** fails when a stored **`task_sha256`** disagrees with the current **`--task`**; legacy **`pipeline.json`** without the field skips that check. Documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py) and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).

- **MCP / tooling:** **`replayt version --format json`** → **`cli_json_stdout_contract`** now includes **`subprocess_stream_semantics`**: UTF-8, a single JSON object on stdout when a listed machine route is active (no NDJSON prefix/suffix), explicit policy that stderr may still carry human hints, and a documented **`ci`** stderr case (exit-code legend). Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **repo-checked-in MCP argv allowlist enforced by `replayt doctor` / `config`** and **stdin tool-dispatch protocol in core** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`ctx.llm.complete_text(..., schema_name=...)`** (optional, whitespace-stripped) copies the label onto **`llm_request`** and **`llm_response`** so freeform completions are filterable the same way as Pydantic contracts. **`replayt runs`**, **`replayt stats`**, and **`replayt inspect`** **`--structured-schema`** now match **`schema_name`** on those LLM lines as well as **`structured_output`** / **`structured_output_failed`**. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py), [`src/replayt/cli/display.py`](src/replayt/cli/display.py), and [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/RECIPES.md`](docs/RECIPES.md), [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **Instructor / PydanticAI inside `LLMBridge`** and **DSPy `compile` as default bridge** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **Enterprise integrator:** **`run_hook`** subprocesses now receive **`REPLAYT_RUN_JSONL`** (resolved path to **`<run_id>.jsonl`** under **`REPLAYT_LOG_DIR`**, same shape as **`resume_hook`**; the file may not exist yet before the first event on a new run) so pre-run policy scripts can reuse resume-style path logic for mount checks, overwrite policy, or shared gate code. **`policy_hook_env_catalog`** lists the name on **`run_hook`**. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); compose-outside **first-class `replayt attest` / notarization registry** (hosted provenance product) and **auto-inject corporate `CORP_*` env vars into every hook** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: policy profiles and cloud upload from hooks** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt init --list`** text output now includes a second copy-paste line per template: explicit **`replayt run <workflow_file> --inputs-file <inputs_file>`** (works before you rely on **`.replaytrc.toml`** defaults). **`replayt.init_templates.v1`** **`cli`** adds **`doctor_target`**, **`validate_explicit`**, **`dry_check_explicit`**, **`run_explicit`**, **`ci_dry_check_explicit`**, project-default **`dry_check`** / **`run`**, and LLM templates add **`dry_run`**. Implemented in [`src/replayt/cli/templates.py`](src/replayt/cli/templates.py) and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); documented in [`docs/CLI.md`](docs/CLI.md) and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt bootstrap` one-shot chained setup** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** **`replayt.__version_tuple__`** (**`(major, minor, patch)`** ints parsed from the leading **`X.Y.Z`** prefix of **`__version__`**) is part of **`replayt.__all__`**, and **`replayt version --format json`** now includes **`replayt_version_tuple`** as **`[major, minor, patch]`** next to **`replayt_version`** so CI and libraries can compare semver without ad hoc string splits. **`docs/PUBLIC_API_CONTRACT.json`** updated via **`scripts/public_api_report.py`**. Compose-outside **upstream Dependabot policy** and **canonical hosted Sphinx/MkDocs site** ideas are logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: hosted API reference from docstrings** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md). Documented in [`README.md`](README.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), and [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_cli.py`](tests/test_cli.py) and [`tests/test_version_metadata.py`](tests/test_version_metadata.py).

- **Product engineer:** **`replayt report`** with **`--style stakeholder`** or **`support`** (HTML and Markdown) now includes a **Stakeholder CLI handoff** section after the action summary: copy-paste commands for **`replayt inspect`**, regenerating the stakeholder report, offline **`replayt replay`**, **`replayt resume TARGET …`** when the run is paused, and **`replayt inspect --output json`** when the run failed. **`bundle-export`** **`report.html`** picks up the same block via the shared report builder. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py) and [`src/replayt/cli/report_template.py`](src/replayt/cli/report_template.py); documented in [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_display.py`](tests/test_display.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt version --format json`** → **`operational_paths`** (`replayt.operational_paths.v1`) now includes **`ci_artifact_readiness`** (same **`name`**, **`ok`**, **`detail`**, **`path`** rows as **`replayt doctor`** for env-driven JUnit XML, **`replayt.ci_run_summary.v1`**, and GitHub / **`REPLAYT_STEP_SUMMARY`** sinks) and **`ci_artifact_readiness_ok`** when every row passes, so CI setup steps can preflight artifact paths without a full **`replayt doctor`** run. Implemented in [`src/replayt/cli/path_readiness.py`](src/replayt/cli/path_readiness.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** **`llm_request`** and **`llm_response`** events from **`ctx.llm.parse(...)`** now include top-level **`schema_name`** (the Pydantic model class name, matching **`structured_output`** / **`structured_output_failed`**) alongside **`schema_sha256`**, so JSONL pipelines can filter provider round trips by contract without waiting for the structured-output line; **`complete_text`** traffic omits the field. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); documented in [`README.md`](README.md), [`docs/RECIPES.md`](docs/RECIPES.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py) and [`tests/test_runner.py`](tests/test_runner.py).

- **Security / compliance:** **`resume_hook`** subprocesses now receive **`REPLAYT_LOG_DIR`** (resolved log root, same as **`run_hook`**) and **`REPLAYT_RUN_JSONL`** (resolved path to **`<run_id>.jsonl`**) so approval gates can read the paused run timeline without reconstructing paths; **`policy_hook_env_catalog`** lists both names. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **Beyond core: LLM gateway allowlists and inline JSONL hash chains** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **built-in `OPENAI_BASE_URL` hostname allowlist inside `LLMBridge` / core** and **per-line Merkle / hash-chain fields on every JSONL event** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Junior onboarding:** **`MODULE:VAR`** targets now reject empty module or variable segments and **more than one colon** in the pair with copy-paste examples (Windows drive-letter paths unchanged). When the module imports but the **attribute is wrong**, errors list exported **`Workflow`** names and suggest **`replayt run`** / **`replayt doctor --skip-connectivity --target …`**, preferring **`wf`** when present. Implemented in [`src/replayt/cli/targets.py`](src/replayt/cli/targets.py); documented in [`docs/CLI.md`](docs/CLI.md); compose-outside **`replayt doctor --upgrade-replayt` / bundled pip upgrade** and **mandatory live LLM smoke test in `doctor`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** **`replayt version --format json`** now includes **`python.implementation`** (**`name`**, dotted **`version`**, optional **`cache_tag`**) and top-level **`platform_machine`** (**`platform.machine()`**) so CI matrices and wrappers can assert CPython vs PyPy and CPU architecture without shelling out to extra Python introspection. Implemented in [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md); compose-outside **HTTP-merged org-wide project config** and **built-in deprecated CLI flag aliases** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.18`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.18 - 2026-03-23

- **Reviewcodebase pass:** Jupyter **`display_run`** and HTML reports coerce values with **`str(...)`** before **`html.escape`** where log payloads may use non-string scalars (for example numeric **`status`** or **`ts`**), avoiding **`TypeError`** when rendering odd or hand-edited JSONL. **`SQLiteStore(..., read_only=True)`** now opens the DB via **`Path.resolve().as_uri()`** plus **`?mode=ro`** so spaces and reserved URI characters in the file path cannot break the connection string. Implemented in [`src/replayt/notebook.py`](src/replayt/notebook.py), [`src/replayt/cli/report_template.py`](src/replayt/cli/report_template.py), and [`src/replayt/persistence/sqlite.py`](src/replayt/persistence/sqlite.py); regressions in [`tests/test_notebook.py`](tests/test_notebook.py) and [`tests/test_sqlite_store.py`](tests/test_sqlite_store.py).

- **Docs:** deslopdoc pass on [`CHANGELOG.md`](CHANGELOG.md) (replace "aligned with" phrasing in release bullets) and [`docs/CLI.md`](docs/CLI.md) (`cli_subcommands` wording in the version JSON section).

- **Improvedoc pass:** **`distribution_metadata`** now treats wheel header values as **`str` only** (whitespace-stripped; otherwise **`null`**) and ignores **`Project-URL`** **`get_all`** results that are not a **list** or **tuple**, so broken or hostile **`importlib.metadata`** payloads cannot yield non-string JSON scalars or crash URL parsing. Implemented in [`src/replayt/cli/distribution_metadata.py`](src/replayt/cli/distribution_metadata.py); regressions in [`tests/test_distribution_metadata.py`](tests/test_distribution_metadata.py).

- **Design fidelity:** Jupyter helpers in [`src/replayt/notebook.py`](src/replayt/notebook.py) no longer load Mermaid or Tailwind from public CDNs; graph and run views use embedded CSS and offline Mermaid source text (**Principle 6: local-first**). Regressions in [`tests/test_notebook.py`](tests/test_notebook.py).

- **Agent harness / automation:** **`scripts/skill_release_loop.py`** and **`scripts/skill_release_loop_agent.py`** record **`skill_command_sha256`** (UTF-8 SHA-256 of the raw **`--skill-command`** template) in **`pipeline.json`** and each **`replayt.skill_invocation.v1`** sidecar; export **`SKILL_COMMAND_SHA256`** and **`{skill_command_sha256}`** for **`--skill-command`** templates. **`--resume`** fails when the stored digest disagrees with the current template (legacy **`pipeline.json`** without the field is ignored for that check). Sidecars and env add repo-relative **`prompt_file_rel`**, **`log_file_rel`**, **`run_dir_rel`** plus **`SKILL_PROMPT_REL`**, **`SKILL_LOG_REL`**, **`SKILL_RUN_DIR_REL`**. Documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py) and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).

- **MCP / tooling:** **`replayt version --format json`** → **`cli_json_stdout_contract`** now includes sorted **`trust_profiles`** (human-readable side-effect summaries) and a **`trust_profile`** id on every JSON-on-stdout route so subprocess and MCP wrappers can map allowlisted argv to seal hooks, run execution, dry-check validation, or read-only inventory without scraping `--help`. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **upstream-shipped `examples/mcp_stdio_server/` (or similar) reference MCP implementation** and **stdin JSON batch router for multiple replayt invocations** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`replayt diff`**, **`replayt report`**, and **`replayt report-diff`** accept repeatable **`--llm-model MODEL`** (same exact-match rules as **`replayt inspect`**: **`effective.model`** with legacy top-level **`model`** on **`structured_output`**, **`structured_output_failed`**, and **`llm_response`**). Diff JSON (**`replayt.diff_report.v1`**) adds optional **`llm_model_filter`** when set; structured-output counts and LLM latency sums use the filtered slice while states, status, and tool-call counts stay full-run. Reports add a short header note and limit structured-output sections, parse-failure cards, and token totals to the same slice. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py), [`src/replayt/cli/report_template.py`](src/replayt/cli/report_template.py), [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`docs/CLI.md`](docs/CLI.md), **Pattern: framework in a sandbox step** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **Finding runs by logged model id** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt replay --llm-model`** timeline masking, **canonical OpenAI `tool_calls` JSON export for LangChain adapters from core**, and **LangChain `BaseMessage` / message-list ↔ `RunContext` conversion in core** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Enterprise integrator:** Trusted policy hooks (`run_hook`, `resume_hook`, `export_hook`, `seal_hook`, `verify_seal_hook`) now receive **`REPLAYT_REPLAYT_VERSION`** (installed **replayt** version string; always overrides any same-named variable in the parent environment). **`policy_hook_env_catalog`** lists the new name on every hook. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **Beyond core: policy profiles and cloud upload from hooks** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`[tool.replayt]` policy profiles / multi-hook argv tables** and **first-class `replayt export-run --s3-uri` (or similar) cloud upload** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt init --list`** prints every **`--template`** key with a one-line summary and a copy-paste **`replayt init --template KEY`** line; **`--list --output json`** emits **`replayt.init_templates.v1`** (per-template **`workflow_file`**, **`inputs_file`**, **`llm_backed`**, **`summary`**, **`cli`**: **`init_here`**, **`init_with_ci_github`**). Registered under **`cli_machine_readable_schemas.init_templates`** and **`cli_json_stdout_contract`**. Implemented in [`src/replayt/cli/templates.py`](src/replayt/cli/templates.py) and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); documented in [`docs/CLI.md`](docs/CLI.md) and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **implicit `./inputs.json` when flags are omitted** and **`replayt init --open` / OS editor launch after scaffold** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** **`distribution_metadata`** (`replayt.distribution_metadata.v1`) on **`replayt version --format json`** now includes wheel PEP 566 **`Summary`**, **`License`**, and a sorted **`project_urls`** array of **`label` / `url`** objects parsed from **`Project-URL`** headers (empty array when none are published). When no distribution record exists, those fields are **`null`** alongside existing **`ok: false`**. Implemented in [`src/replayt/cli/distribution_metadata.py`](src/replayt/cli/distribution_metadata.py); documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md); compose-outside **`replayt credits` / maintainer roster CLI** and **bundled codespell or typos in `maintainer_checks.py`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Beyond core: maintainer credits CLI and prose linters** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_distribution_metadata.py`](tests/test_distribution_metadata.py).

- **Product engineer:** **`replayt replay`** accepts **`--style default|stakeholder|support`**, using the same **`--style`** rules as **`replayt report`**. Non-default styles omit **`llm_*`** and **`tool_*`** timeline rows in text and HTML; HTML adds the same **`attention=`** / **`attention_kind=`** banner as **`replayt runs`**. **`bundle-export`** **`timeline.html`** now follows **`--report-style`**, and **`manifest.json`** records **`timeline_style`** (export seal **`extra`** includes it too). **`inspect --output markdown`** suggests **`replayt replay … --style stakeholder`**. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py), [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and **Beyond core: failure banners and spreadsheet-style run lists** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **automatic stderr report hints after every failed `replayt run`** and **first-class `replayt runs --format csv`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_display.py`](tests/test_display.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt.ci_run_summary.v1`** (from **`replayt ci --summary-json`**, **`replayt run`** with **`REPLAYT_SUMMARY_JSON`**, or env **`REPLAYT_SUMMARY_JSON`**) now includes **`replayt_version`**, **`python_version`**, and **`platform`** so archived CI artifacts stay self-describing across image and interpreter upgrades. Implemented in [`src/replayt/cli/ci_artifacts.py`](src/replayt/cli/ci_artifacts.py); documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **systemd `sd_notify` from `replayt ci`** and **NDJSON / structured stderr progress during `replayt ci`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** **`structured_output_failed`** events with **`stage`** **`schema_limit`** (JSON Schema exceeds **`max_schema_json_chars`** before any HTTP call) now include the same resolved **`effective`** object as other parse failures—**`model`**, sampling fields, optional **`experiment`**, and **`structured_output_mode`**—so **`jq`** / DuckDB filters can attribute oversized-schema rejects without joining **`llm_request`**. **`messages_sha256`** / **`effective_sha256`** stay absent when no provider round trip occurred. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); documented in [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), [`README.md`](README.md), and [`docs/RECIPES.md`](docs/RECIPES.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).

- **Security / compliance:** Trusted policy hooks (`run_hook`, `resume_hook`, `export_hook`, `seal_hook`, `verify_seal_hook`) now receive a small **logging contract** in the subprocess environment: **`REPLAYT_FORBID_LOG_MODE_FULL`** (`1` / `0`), **`REPLAYT_REDACT_KEYS_JSON`** (sorted JSON array of configured redact field names, never values), and **`REPLAYT_LOG_MODE`** on **every** hook (including **`resume_hook`**, which previously lacked log-mode context). **`policy_hook_env_catalog`** in **`replayt version --format json`** lists the new names. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py), and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); compose-outside **FIPS/HSM-mandated seal primitives** and **SOC2/ISO control-id tagging on every JSONL line** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py) and [`tests/test_security.py`](tests/test_security.py).

- **Junior onboarding:** **`MODULE:VAR`**, **`.py`**, and **`.yaml`** targets that resolve to a non-**`Workflow`** value now report the Python type and copy-paste **`replayt doctor --skip-connectivity --target …`** guidance instead of a one-line type error. Missing **PyYAML** for **`.yaml` / `.yml`** paths surfaces the existing **`pip install replayt[yaml]`** message as a **`typer.BadParameter`** with a retry hint. Implemented in [`src/replayt/cli/targets.py`](src/replayt/cli/targets.py); documented in [`docs/CLI.md`](docs/CLI.md); compose-outside **`replayt env` / env-print** and fuzzy subcommand spelling logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with tutorial follow-up in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** **`replayt version --format json`** now includes **`distribution_metadata`** (`replayt.distribution_metadata.v1`): wheel **`Version`** and **`Requires-Python`** from **`importlib.metadata`** when the **`replayt`** distribution is installed, plus **`ok`** / **`detail`** when only a source tree is on **`PYTHONPATH`**. Registered under **`cli_machine_readable_schemas.distribution_metadata`**. Implemented in [`src/replayt/cli/distribution_metadata.py`](src/replayt/cli/distribution_metadata.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.17`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.17 - 2026-03-23

- **Reviewcodebase pass:** **`JSONLStore.load_events`**, **`append_event`**, and **`append`** now catch **`UnicodeDecodeError`** on UTF-8 text reads (invalid bytes in the log file) and raise the same style of **`RuntimeError`** as malformed JSON lines, instead of surfacing a raw decode error. Implemented in [`src/replayt/persistence/jsonl.py`](src/replayt/persistence/jsonl.py); regressions in [`tests/test_jsonl_store.py`](tests/test_jsonl_store.py).

- **Docs:** deslopdoc pass on [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md) (replace "aligns with principles" phrasing; tighten brainstorm row 1 and seal item wording), [`docs/CONFIG.md`](docs/CONFIG.md) (remove duplicated "same payload" sentence in the version-report intro), [`docs/CLI.md`](docs/CLI.md) (`validate` exit wording), [`docs/RECIPES.md`](docs/RECIPES.md) (`structured_output` sentence), [`src/replayt/types.py`](src/replayt/types.py) (`LogMode` docstring), and [`.cursor/skills/createfeatures/SKILL.md`](.cursor/skills/createfeatures/SKILL.md) ("optimize for" wording).

- **Improvedoc pass:** **`first_jsonl_event_with_type`** (and callers such as **`run_started_hook_json_blobs_from_jsonl_path`** and standalone **`seal`** / **`verify-seal`** contract extraction) now catches **`UnicodeDecodeError`** on UTF-8 JSONL reads, matching **`OSError`**, so corrupt or non-UTF-8 logs fail closed with **`None`** instead of crashing the CLI. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py); regression in [`tests/test_cli.py`](tests/test_cli.py).

- **Design fidelity:** **`run_started_hook_json_blobs_from_jsonl_path`** and standalone **`seal`** / **`verify-seal`** contract extraction now locate the first **`run_started`** line with a **streaming JSONL read** instead of loading the whole log into memory, keeping large-run inspect and policy-hook paths practical (**Principle 5: replay / local logs**). Implemented via **`first_jsonl_event_with_type`** in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); regression in [`tests/test_cli.py`](tests/test_cli.py).

- **Agent harness / automation:** **`scripts/skill_release_loop.py`** and **`scripts/skill_release_loop_agent.py`** now write **`pipeline.json`** once per run directory (**`replayt.skill_release_pipeline.v1`**: ordered resolved **`--skills`**, **`pipeline_sha256`**, loop task) and export **`SKILL_PIPELINE_SHA256`** plus **`{pipeline_sha256}`** for **`--skill-command`**; each **`replayt.skill_invocation.v1`** sidecar includes **`pipeline_sha256`**. **`--resume`** fails fast when the skill order no longer matches **`pipeline.json`**. Registered under **`maintainer_script_schemas.skill_release_pipeline`** in **`replayt version --format json`**. Documented in [`CONTRIBUTING.md`](CONTRIBUTING.md), [`docs/CLI.md`](docs/CLI.md), and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py), and [`tests/test_cli.py`](tests/test_cli.py).

- **MCP / tooling:** **`replayt version --format json`** now includes **`cli_json_stdout_contract`**: per subcommand, which **`--output` / `--format` / `--json`** flags select JSON on stdout and the matching **`cli_machine_readable_schemas`** key (stdout only; omits file sinks and non-JSON commands). Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`replayt machine-read`** router and **`replayt argv-schema`** introspection dumps logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`replayt inspect RUN_ID`**, **`replayt runs`**, and **`replayt stats`** now accept repeatable **`--llm-model MODEL`** (exact match on logged model id from **`effective.model`** when present, else top-level **`model`** on **`llm_request`**, **`llm_response`**, **`structured_output`**, and **`structured_output_failed`**; OR semantics). JSON **`replayt.inspect_report.v1`** adds optional **`llm_model_filter`** when set; **`replayt.runs_report.v1`** **`filters`** includes **`llm_model`**. **`--output markdown`** and **`--print-inputs`** reject **`--llm-model`** like the other timeline filters. Implemented in [`src/replayt/cli/display.py`](src/replayt/cli/display.py) and [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md), **Pattern: framework in a sandbox step** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **Finding runs by logged model id** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Enterprise integrator:** **`resume_hook`**, **`export_hook`**, **`seal_hook`**, and **`verify_seal_hook`** now receive optional **`REPLAYT_RUN_METADATA_JSON`**, **`REPLAYT_RUN_TAGS_JSON`**, and **`REPLAYT_RUN_EXPERIMENT_JSON`** when the run log's first **`run_started`** records those objects (same sorted JSON strings as **`run_hook`**), so org policy scripts can gate approvals, exports, and seals on promotion metadata without re-parsing JSONL. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py), and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); **`policy_hook_env_catalog`** updated; documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and **Pattern: workflow contract allowlist in policy hooks** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt try --list`** now prints a final copy-paste line per example (**`replayt try --example KEY`**), and **`--list --output json`** (**`replayt.try_examples.v1`**) adds a per-example **`cli`** object with **`try_offline`**, **`try_live`**, **`try_dry_check`**, and **`copy_to_dot`** strings. Implemented via **`packaged_example_cli_snippets()`** in [`src/replayt_examples/catalog.py`](src/replayt_examples/catalog.py) and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py); documented in [`docs/CLI.md`](docs/CLI.md) and the tutorial intro in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** Added **`scripts/changelog_gate_policy.py`** (**`--format json`**, schema **`replayt.changelog_gate_policy.v1`**) as the single source of truth for which paths require **`CHANGELOG.md`** on GitHub pull requests (same rules as **`scripts/check_changelog_if_needed.py`**). Registered under **`maintainer_script_schemas.changelog_gate_policy`** in **`replayt version --format json`**, included in **`scripts/maintainer_checks.py`** (with **`--skip-changelog-gate-policy`** for partial trees), and documented in [`CONTRIBUTING.md`](CONTRIBUTING.md), [`README.md`](README.md), and [`docs/CLI.md`](docs/CLI.md). Regressions in [`tests/test_changelog_gate.py`](tests/test_changelog_gate.py), [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py), and [`tests/test_cli.py`](tests/test_cli.py).

- **Product engineer:** **`replayt report-diff`** now accepts **`--style default|stakeholder|support`**, using the same **`--style`** rules as **`replayt report`**. Stakeholder mode adds the same **`attention=`** / **`attention_kind=`** hints on each run card; support mode moves failure and approval diffs before structured outputs and run context for triage handoffs; HTML footers for non-default styles point back to **`--style default`**. Implemented in [`src/replayt/cli/report_template.py`](src/replayt/cli/report_template.py) and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); documented in [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt version --format json`** now includes **`operational_paths`** (`replayt.operational_paths.v1`): absolute **`cwd`**, **`effective_log_dir`** (same default **`log_dir`** resolution as **`replayt run`** / **`replayt doctor`**: **`[tool.replayt] log_dir`**, **`REPLAYT_LOG_DIR`**, or **`.replayt/runs`**), **`step_summary`** path plus **`path_source`**, and env-only **`ci_artifact_paths`** for **`REPLAYT_JUNIT_XML`** / **`REPLAYT_SUMMARY_JSON`**. Implemented in [`src/replayt/cli/path_readiness.py`](src/replayt/cli/path_readiness.py), [`src/replayt/cli/ci_artifacts.py`](src/replayt/cli/ci_artifacts.py), and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and **Beyond core: disk quotas and Prometheus-style metrics in CI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside **`doctor`** disk gates and **`replayt ci` OpenMetrics** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** Successful **`structured_output`** events now include the resolved **`effective`** object (same shape as on **`llm_request`** / **`llm_response`**) alongside existing completion metadata, so JSONL-only analytics can filter on **`model`**, sampling knobs, and **`experiment`** without joining prior LLM lines. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); documented in [`README.md`](README.md), [`docs/RECIPES.md`](docs/RECIPES.md), [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), [`src/replayt/types.py`](src/replayt/types.py), and **Finding runs that logged a structured output schema** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py) and [`tests/test_runner.py`](tests/test_runner.py).

- **Security / compliance:** On POSIX hosts, **`replayt doctor`** and **`replayt config --format json`** emit soft **`trust_policy_hook_script_*`** checks for on-disk paths referenced by **`run_hook`**, **`resume_hook`**, **`export_hook`**, **`seal_hook`**, and **`verify_seal_hook`** argv (direct script paths and common **`python … script.py`** / **`bash … script.sh`** shapes; standard **`python`** / **`bash`** binaries under well-known **`bin`** prefixes are skipped). Uses path and mode bits only, like workflow entry and inputs JSON audits. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py), [`src/replayt/security.py`](src/replayt/security.py), [`src/replayt/cli/commands/doctor.py`](src/replayt/cli/commands/doctor.py), and [`src/replayt/cli/commands/config_cmd.py`](src/replayt/cli/commands/config_cmd.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and [`docs/CONFIG.md`](docs/CONFIG.md); regressions in [`tests/test_security.py`](tests/test_security.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **Junior onboarding:** `MODULE:VAR` targets that fail during import with **`ImportError`** other than a missing top-level module, or with **`SyntaxError`** in the loaded package, now get the same style of copy-paste hints as **`ModuleNotFoundError`** (including `python -c "import …"`, `python -m py_compile …`, and `replayt doctor --skip-connectivity --target …`). Implemented in [`src/replayt/cli/targets.py`](src/replayt/cli/targets.py); documented in [`docs/CLI.md`](docs/CLI.md); compose-outside alternatives for **`replayt open-docs`** / **`replayt which`** logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md) with **Install path and docs without extra `replayt` commands** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** `replayt version --format json` now includes **`project_config_discovery`** (`replayt.project_config_discovery.v1`), a cwd-independent description of how `.replaytrc.toml` and `pyproject.toml` `[tool.replayt]` are discovered and ordered. When both exist in the same directory, **`replayt config --format json`** lists **`project_config.shadowed_sources`** (absolute paths to ignored `pyproject.toml` files), and **`replayt doctor`** adds a soft **`project_config_shadowed_sources`** check. Implemented in [`src/replayt/cli/config.py`](src/replayt/cli/config.py), [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py), [`src/replayt/cli/commands/config_cmd.py`](src/replayt/cli/commands/config_cmd.py), and [`src/replayt/cli/commands/doctor.py`](src/replayt/cli/commands/doctor.py); documented in [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/CLI.md`](docs/CLI.md), and **Beyond core: strict project-config checks** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); related compose-outside ideas logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.16`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.16 - 2026-03-22

- **Reviewcodebase pass:** Maintainer scripts now tie **`git`** / **`npm install`** subprocess stdin to **`subprocess.DEVNULL`** where the child does not consume stdin ([`scripts/check_changelog_if_needed.py`](scripts/check_changelog_if_needed.py), [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py), [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py), [`scripts/run_codex_skill.py`](scripts/run_codex_skill.py), [`scripts/run_light_release_skill.py`](scripts/run_light_release_skill.py)) so CI hosts or wrappers that forward an attached stdin do not block or feed unexpected input. **`run_light_release_skill.py`** exits with a clear message when **`REPO_ROOT`** is unset. Regressions in [`tests/test_changelog_gate.py`](tests/test_changelog_gate.py), [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py), [`tests/test_run_codex_skill.py`](tests/test_run_codex_skill.py), and [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).

- **Docs:** deslopdoc pass on [`docs/CLI.md`](docs/CLI.md) (`inspect` contract digest and filter wording, `report` header line), [`README.md`](README.md) (fork hygiene sentence), **Finding runs that logged a structured output schema** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md), and a header docstring in [`src/replayt/cli/report_template.py`](src/replayt/cli/report_template.py).

- **Improvedoc pass:** **`scripts/pyproject_pep621_report.py`** now treats **`tomllib` / `tomli` `TOMLDecodeError`** as a handled load failure (alongside **`OSError`**, **`UnicodeDecodeError`**, and **`ValueError`**) so malformed **`pyproject.toml`** returns **`ok: false`** on Python 3.10 instead of exiting uncaught; regression in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).

- **Design fidelity:** `run_started.runtime.llm` now records **`http_retries`** (OpenAI-compat transport retry budget) alongside other non-secret LLM settings so logs stay self-describing when transient HTTP retries occur ([`src/replayt/runner.py`](src/replayt/runner.py), [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); regression in [`tests/test_runner.py`](tests/test_runner.py)).

- **Agent harness / automation:** **`scripts/skill_release_loop.py`** and **`scripts/skill_release_loop_agent.py`** now export **`SKILL_STEP_INDEX`** and **`SKILL_STEP_TOTAL`** (1-based position in the configured skill list per iteration; fix prompts use **`0`/`0`**) and write the same fields on **`replayt.skill_invocation.v1`** sidecars; **`--skill-command`** templates support **`{step_index}`** / **`{step_total}`**; generated skill prompts include a **`Pipeline step:`** line. Documented in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Beyond core: skill-release loop wrappers and sidecar integrity** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); nested-parent env injection and bundled **`prompt_sha256`** on sidecars are logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md). Regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py) and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).

- **MCP / tooling:** **`replayt version --format json`** now includes **`cli_stdio_contract`**: which subcommands may read a UTF-8 JSON inputs object from **process stdin** when **`--inputs-file -`**, **`--inputs-json @-`**, or **`REPLAYT_INPUTS_FILE=-`** applies, plus a **`devnull`** default recommendation so subprocess and MCP hosts do not accidentally forward attached stdin into **`run`** / **`ci`** / **`validate`** / **`doctor`**. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`replayt inspect RUN_ID`** now accepts repeatable **`--structured-schema NAME`** (exact match on **`structured_output`** / **`structured_output_failed`** payload **`schema_name`**; OR semantics), with the same filter rules as **`replayt runs`** and **`replayt stats`**. JSON **`replayt.inspect_report.v1`** adds optional **`structured_schema_filter`** when set; **`--output markdown`** rejects **`--structured-schema`** like the other timeline filters. Implemented in [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md), **Pattern: framework in a sandbox step** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and **Finding runs that logged a structured output schema** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Enterprise integrator:** **`export_hook`**, **`seal_hook`**, and **`verify_seal_hook`** subprocesses now receive **`REPLAYT_WORKFLOW_CONTRACT_SHA256`**, **`REPLAYT_WORKFLOW_NAME`**, and **`REPLAYT_WORKFLOW_VERSION`** parsed from the run's **`run_started`** event (same digest as **`run_started.runtime.workflow.contract_sha256`**), and **`export_hook`** also receives **`REPLAYT_TARGET`** when **`--target`** is passed to **`export-run`** / **`bundle-export`**, so org allowlist scripts can gate archives and seal verification without re-parsing JSONL. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/export.py`](src/replayt/cli/commands/export.py); **`policy_hook_env_catalog`** in **`replayt version --format json`** updated accordingly. Documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and **Pattern: workflow contract allowlist in policy hooks** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`replayt inspect RUN_ID --print-inputs`** prints the logged **`run_started.inputs`** object as compact JSON (stdout) so you can rerun with **`replayt run TARGET --inputs-json …`** without hand-copying JSONL. Implemented in [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py); documented in [`docs/CLI.md`](docs/CLI.md) and **Reuse inputs from a finished run** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** Added **`scripts/pyproject_pep621_report.py`** (**`--format json`**, schema **`replayt.pyproject_pep621_report.v1`**) for a stable, sorted snapshot of **`[project]`** **`dependencies`** and **`optional-dependencies`** so forks can diff pins in CI; wired into **`scripts/maintainer_checks.py`** (with **`--skip-pyproject-pep621`** for partial trees), registered under **`maintainer_script_schemas.pyproject_pep621`** in **`replayt version --format json`**, and documented in [`CONTRIBUTING.md`](CONTRIBUTING.md), [`README.md`](README.md), and [`docs/CLI.md`](docs/CLI.md). Python 3.10 contributor installs pull optional dev **`tomli`**; 3.11+ uses **`tomllib`**. Regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **Product engineer:** **`replayt report`** HTML and Markdown headers now include the same **`attention=`** / **`attention_kind=`** line as **`replayt runs`** text output (via **`run_attention_summary`**), so stakeholder reports and **`bundle-export`** HTML match the run inventory. Implemented in [`src/replayt/cli/report_template.py`](src/replayt/cli/report_template.py); documented in [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** **`replayt version --format json`** now includes **`cli_exit_codes`**: machine-readable **`replayt run` / `ci` / `resume` / `try`** exit 0/1/2 semantics (same as **`replayt.ci_run_summary.v1`** **`exit_code`**), JSON-mode exits for **`doctor`** and **`validate`**, and a short note that other helpers reserve **2** for paused workflow runs. Implemented in [`src/replayt/cli/run_support.py`](src/replayt/cli/run_support.py) and [`src/replayt/cli/commands/version_cmd.py`](src/replayt/cli/commands/version_cmd.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and [`docs/CONFIG.md`](docs/CONFIG.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** Successful **`structured_output`** events now include **`usage`**, **`latency_ms`**, and **`finish_reason`** from the same chat completion (plus optional **`chat_completion_id`** / **`system_fingerprint`** when the gateway returns them), matching the preceding **`llm_response`** so experiment and cost aggregates can key off one event type. Implemented in [`src/replayt/llm.py`](src/replayt/llm.py); documented in [`README.md`](README.md), [`docs/RECIPES.md`](docs/RECIPES.md), [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), [`src/replayt/types.py`](src/replayt/types.py), and **Finding runs that logged a structured output schema** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); compose-outside alternatives for usage normalization and renamed token-limit fields are logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py) and [`tests/test_runner.py`](tests/test_runner.py).

- **Security / compliance:** On POSIX hosts, **`replayt doctor`** and **`replayt config --format json`** emit soft **`trust_inputs_file_*`** checks for default or preflight **inputs JSON** files that are group- or world-readable/writable when those paths exist on disk (path and mode bits only; stdin defaults are skipped). Implemented in [`src/replayt/security.py`](src/replayt/security.py), [`src/replayt/cli/config.py`](src/replayt/cli/config.py), [`src/replayt/cli/commands/doctor.py`](src/replayt/cli/commands/doctor.py), and [`src/replayt/cli/commands/config_cmd.py`](src/replayt/cli/commands/config_cmd.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and [`docs/CONFIG.md`](docs/CONFIG.md); regressions in [`tests/test_security.py`](tests/test_security.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **Security / compliance:** On POSIX hosts, **`replayt doctor --target`** and **`replayt config --format json`** (when **`run.default_target`** is set) emit soft **`trust_workflow_entry_*`** checks for group- or world-readable/writable workflow entry files—the `.py` / YAML path you pass or the imported module's **`__file__`** for **`MODULE:VAR`**—using path and mode bits only (no file reads). Implemented in [`src/replayt/security.py`](src/replayt/security.py), [`src/replayt/cli/targets.py`](src/replayt/cli/targets.py), [`src/replayt/cli/commands/doctor.py`](src/replayt/cli/commands/doctor.py), and [`src/replayt/cli/commands/config_cmd.py`](src/replayt/cli/commands/config_cmd.py); documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and [`docs/CONFIG.md`](docs/CONFIG.md); regressions in [`tests/test_security.py`](tests/test_security.py) and [`tests/test_cli.py`](tests/test_cli.py).

- **Junior onboarding:** When the default inputs JSON path is missing, `replayt run` / `ci` / `validate` / `doctor --target` now say whether it came from **`[tool.replayt] inputs_file`**, **`REPLAYT_INPUTS_FILE`**, or an explicit **`--inputs-file`** / **`--inputs-json @path`**, with copy-paste fixes (`replayt config`, edit config, or override flags). Covered in [`src/replayt/cli/validation.py`](src/replayt/cli/validation.py), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** `replayt version --format json` now includes sorted **`cli_subcommands`**, the top-level Typer command names actually registered on the installed CLI, so CI and MCP-style wrappers can allowlist argv without scraping help text. Documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and **Beyond core: shell completion and CLI discovery for wrappers** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Maintainer hygiene:** keep `## Unreleased` non-empty after tagging **`0.4.15`** so `scripts/maintainer_checks.py --changelog-nonempty` and pre-tag GitHub Actions verification pass on the default branch.

## 0.4.15 - 2026-03-22

- **Reviewcodebase pass:** [`scripts/verify_github_action.py`](scripts/verify_github_action.py) now resolves the git work tree with `git rev-parse --show-toplevel` and runs every `git` / `gh` subprocess with that `cwd`, so triggering CI still targets the intended repository when the shell is outside the tree; subprocess stdin is tied to `DEVNULL`, `gh run list` JSON is parsed defensively, and [`tests/test_verify_github_action.py`](tests/test_verify_github_action.py) covers `resolve_git_repo_root` success and failure.

- **Docs:** deslopdoc pass on [`docs/CLI.md`](docs/CLI.md) (`replayt report` `--style` wording, `log-schema` line), [`docs/CONFIG.md`](docs/CONFIG.md) (drop "quickest way" hedge), [`docs/README.md`](docs/README.md) and [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) (plainer maintainer-review blurb and verdict), [`src/replayt_examples/README.md`](src/replayt_examples/README.md) (tutorial sentence on non-LLM steps), and maintainer-loop header comments in [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py) / [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py) plus default task text ("full repo review" instead of "comprehensive review").

- **Improvedoc pass:** `replayt inspect` text replay and HTML timeline rendering no longer crash when JSONL events omit `seq` or use a non-integer value; `format_timeline_seq` in [`src/replayt/cli/display.py`](src/replayt/cli/display.py) mirrors the same formatting used by [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py). Stdin JSON reads for `--inputs-file -` / `@-` now catch `OSError` only instead of a bare `Exception`. Regressions in [`tests/test_display.py`](tests/test_display.py).

- **Design fidelity:** `replayt inspect --output markdown` no longer emits a literal `{run_id}` in the paused-run “see `replayt inspect`” hint when no approval id is available; the hint now interpolates the real run id so copy-paste replay workflows stay accurate ([`src/replayt/cli/display.py`](src/replayt/cli/display.py); regression in [`tests/test_display.py`](tests/test_display.py)).

- **Agent harness / automation:** `scripts/skill_release_loop.py` and `scripts/skill_release_loop_agent.py` now export **`SKILL_RUN_DIR`** (absolute timestamped run directory), add **`run_dir`** and sorted **`injected_env_keys`** to each **`*.invocation.json`**, and support **`{run_dir}`** / **`{run_dir_q}`** in **`--skill-command`** templates so backends can anchor correlated artifacts and validate the env contract without duplicating key lists. Documented in [`CONTRIBUTING.md`](CONTRIBUTING.md); regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py) and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).

- **MCP / tooling:** **`replayt version --format json`** now includes **`policy_hook_env_catalog`**: per-hook **`argv_env`** / **`argv_config_key`** pairs, sorted **`injected_env_vars`** for each trusted policy subprocess, and **`subprocess_stdin`**: **`devnull`**, so MCP servers and CI wrappers can validate hook contracts without scraping docs. Documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and **Beyond core: MCP servers wrap the CLI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Framework enthusiast:** **`replayt inspect RUN_ID`** now accepts repeatable **`--tool NAME`** (exact match on **`tool_call`** payload **`name`**; OR semantics), with the same filter semantics as **`replayt runs`** and **`replayt stats`**. JSON **`replayt.inspect_report.v1`** adds optional **`tool_name_filter`** when set; **`--output markdown`** rejects **`--tool`** like the other timeline filters. Documented in [`docs/CLI.md`](docs/CLI.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Enterprise integrator:** **`run_hook`** and **`resume_hook`** subprocesses now receive **`REPLAYT_WORKFLOW_CONTRACT_SHA256`**, **`REPLAYT_WORKFLOW_NAME`**, and **`REPLAYT_WORKFLOW_VERSION`** from the resolved target's **`Workflow.contract()`** (the same digest recorded on **`run_started`**), so org policy wrappers can allowlist workflow surfaces before runs and before **`approval_resolved`** is written. Documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and **Pattern: workflow contract allowlist in policy hooks** in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Startup IC:** **`--inputs-file -`** and **`--inputs-json @-`** read a UTF-8 JSON inputs object from **stdin** (empty or whitespace-only stdin becomes `{}`); env **`REPLAYT_INPUTS_FILE=-`** does the same when CLI input flags are omitted. **`replayt config --format json`** reports **`run.default_inputs_file`** as **`-`** with source **`env:REPLAYT_INPUTS_FILE (stdin)`** in that case. Documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Open-source maintainer:** `replayt version --format json` now includes sorted **`maintainer_script_schemas`**, mapping stable ids for repo **`scripts/*.py`** machine-readable reports (`changelog_unreleased`, `docs_index`, version/catalog/API contracts, **`maintainer_checks`**, and skill-loop **`*.invocation.json`**) so forks and CI can validate script output against the installed wheel without scraping Python sources. Documented in [`README.md`](README.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), and [`docs/CLI.md`](docs/CLI.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Product engineer:** `replayt inspect RUN_ID --output markdown` emits a short paste-friendly stakeholder summary (workflow, status, contract digest when logged, last event time, the same attention line as `replayt runs`, plus copy-paste `replayt report` / `replayt resume` hints). Markdown summarizes the full run only; it cannot be combined with `--event-type`, `--note-kind`, or `--finish-reason`. Documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **DevOps / SRE:** `replayt config --format json` and `replayt doctor --format json` now resolve the markdown step-summary sink the same way as `replayt run` / `replayt ci`: **`GITHUB_STEP_SUMMARY`** if set, otherwise **`REPLAYT_STEP_SUMMARY`**, so non-GitHub CI jobs that only set the latter pass **`ci_github_summary_ready`** preflight. Documented in [`docs/CONFIG.md`](docs/CONFIG.md) and [`docs/RECIPES.md`](docs/RECIPES.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **ML / LLM engineer:** `structured_output_failed` events for Pydantic `schema_validate` failures now include bounded **`validation_issues`** (path `loc`, `msg`, issue `type`), plus **`validation_issue_count`** and **`validation_issues_truncated`** when the model returns more violations than are logged, so JSONL and `replayt inspect` workflows can triage bad structured outputs without opening full responses. Documented in [`README.md`](README.md), [`docs/RECIPES.md`](docs/RECIPES.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), with regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).

- **Security / compliance:** Optional **`forbid_log_mode_full`** in **`[tool.replayt]`** / **`.replaytrc.toml`** and env **`REPLAYT_FORBID_LOG_MODE_FULL`** reject **`log_mode=full`** on **`replayt run`**, **`ci`**, **`try`**, and **`resume`** after defaults resolve (falsy env strings disable the policy even when the config key is true). **`replayt config --format json`** reports **`runtime_defaults.log_mode_full_forbidden`**, and **`replayt doctor --format json`** adds **`log_mode_full_forbidden`** when the default resolved mode would violate the policy. Documented in [`README.md`](README.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/CLI.md`](docs/CLI.md), and **Beyond core: immutable log volumes** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); OS-level WORM hooks in core are logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md). Regressions in [`tests/test_cli.py`](tests/test_cli.py).

- **Junior onboarding:** `replayt run` / `ci` / `validate` / `doctor --target` now explain missing `TARGET` strings with copy-paste fixes when you omit the `:` in `MODULE:VAR`, type `workflow` instead of `workflow.py` while that file exists, pass a directory, or point at a non-`.py`/`.yaml` file. Windows absolute paths (`C:\\...`) no longer misparse as `MODULE:VAR` because of the drive-letter colon. Covered in [`src/replayt/cli/targets.py`](src/replayt/cli/targets.py), [`docs/CLI.md`](docs/CLI.md), and [`tests/test_cli.py`](tests/test_cli.py).

- **Staff / platform:** `replayt version --format json` now includes sorted **`supported_project_config_keys`**, matching the `[tool.replayt]` / `.replaytrc.toml` allowlist in the parser, so CI and wrappers can anchor editor schemas or drift checks to the installed package without scraping source; documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).

## 0.4.14 - 2026-03-22

- **Reviewcodebase pass:** When the parent `replayt run --timeout` wrapper fails to append `run_interrupted` after a subprocess timeout, the CLI now logs a warning with `run_id`, log dir, and optional SQLite path instead of failing silently; [`MultiStore.close`](src/replayt/persistence/multi.py) now invokes `close()` on the primary when present (in addition to mirrors) so composite stores do not leak handles. Covered by new CLI and SQLite store regressions.
- **Docs:** deslopdoc pass on CLI help text, module docstrings, HTML/Markdown report copy, [`docs/replayt-demo.cast`](docs/replayt-demo.cast), and maintainer loop progress strings in [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py) / [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py): swap em-dash splices for plain punctuation, fix a broken `replayt contract` help string in [`src/replayt/cli/commands/inspect.py`](src/replayt/cli/commands/inspect.py), and tighten a few tutorial sentences in [`docs/QUICKSTART.md`](docs/QUICKSTART.md) and [`src/replayt_examples/README.md`](src/replayt_examples/README.md).
- **Improvedoc pass:** `Runner.run` now clears per-run `_current_state` at the start of each invocation, emits a `step_error` event before `run_failed` when a step fails after retries, when `max_steps` is exceeded, or when lifecycle hooks raise (matching the existing context-schema path), and documents the real `retry_scheduled` / `step_error` payload shapes in [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); covered by expanded [`tests/test_runner.py`](tests/test_runner.py) regressions.
- **Agent harness / automation:** `scripts/skill_release_loop.py` and `scripts/skill_release_loop_agent.py` now write a machine-readable **`*.invocation.json`** next to each generated **`*.prompt.md`** (schema **`replayt.skill_invocation.v1`**) with resolved `repo_root`, `skill_root`, paths, iteration counters, and task text so CI and custom backends can consume the same contract as the `SKILL_*` environment variables without scraping Markdown; documented in [`CONTRIBUTING.md`](CONTRIBUTING.md), with regressions in [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py).
- **MCP / tooling:** `replayt version --format json` now lists **`bundle_export`**, **`export_bundle`**, **`export_seal`**, **`seal`**, and **`verify_seal_report`** under **`cli_machine_readable_schemas`** so wrappers can discover tarball manifest and seal JSON shapes alongside existing CLI report schemas; documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Framework enthusiast:** `replayt inspect`, `replayt runs`, and `replayt stats` now accept repeatable **`--finish-reason REASON`** filters (exact match on `llm_response` payload `finish_reason`; OR semantics) so agent-style workflows can triage token-limit stops without raw JSONL greps; JSON **`replayt.inspect_report.v1`** adds optional **`finish_reason_filter`**, and **`replayt.runs_report.v1`** includes **`filters.finish_reason`**; documented in [`docs/CLI.md`](docs/CLI.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Maintainer hygiene:** keep this section non-empty while the working tree contains unreleased changes so `scripts/maintainer_checks.py --changelog-nonempty` and the pre-tag GitHub Actions verification succeed on the live repository state.
- **Open-source maintainer:** `scripts/public_api_report.py` now mirrors the example-catalog contract flow with `--snapshot-out` / `--check`, `docs/PUBLIC_API_CONTRACT.json` pins the top-level `replayt` export surface for semver review, and `scripts/maintainer_checks.py` now fails on public API contract drift instead of only missing `__all__` symbols; covered by expanded maintainer-script regressions and maintainer-doc updates.
- **ML / LLM engineer:** `LLMBridge` now emits stable `messages_sha256` / `effective_sha256` fingerprints on `llm_request` and `llm_response`, plus `schema_sha256` on structured parse flows (`llm_request`, `llm_response`, `structured_output`, and `structured_output_failed`), so redacted JSONL still supports prompt/schema/settings comparisons across experiments; documented in [`README.md`](README.md), [`docs/RECIPES.md`](docs/RECIPES.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), with regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py) and [`tests/test_runner.py`](tests/test_runner.py).
- **Security / compliance:** `replayt doctor --format json` and `replayt config --format json` now flag POSIX log directories and nearby `.env` files that are group-readable or group-writable, not only world-accessible, so shared Unix groups show up as explicit soft trust-boundary findings before regulated data lands in JSONL.
- **Staff / platform:** `Workflow.contract()` now includes a stable `contract_sha256` fingerprint, `run_started.runtime.workflow` records that contract schema + digest on every run, and `replayt contract` / `replayt inspect` surface the same value so upgrade checks can anchor to the exact workflow surface that produced a run; covered by new runner and CLI regressions.
- **Agent harness tooling:** `scripts/run_codex_skill.py` now keeps repo-local `.cursor/skills` as the canonical default even when ambient `SKILL_ROOT` is set, and only adds an external skill directory to `codex exec` when the caller explicitly points outside the repo; covered by repaired `tests/test_run_codex_skill.py` expectations.
- **Junior onboarding:** Python file targets such as `replayt run workflow.py` now fail with actionable setup hints when top-level imports are missing or the file has a syntax error, instead of bubbling a generic loader failure; the CLI points at the missing module or parser line, suggests `pip install -e .` / `MODULE:VAR` where relevant, and is covered by new CLI regressions plus install/CLI doc updates.
- **DevOps / SRE:** `replayt runs` now supports `--older-than` / `--newer-than` age filters and `--output json` with stable schema **`replayt.runs_report.v1`**, so cron jobs and CI wrappers can alert on stale paused/failed runs without scraping terminal text; `replayt version --format json` now advertises the new schema id, and the CLI/README docs call out the age-filtered inventory flow.
- **Product engineer:** `replayt runs` now surfaces a stakeholder-facing action summary for paused and failed runs: text output appends `attention=...`, and `--output json` adds `attention_kind`, `attention_summary`, `pending_approvals`, `latest_failure`, and `latest_structured_output_failure` so PM/support wrappers can triage backlog without opening every HTML report.
- **Startup IC:** `replayt init` and `replayt try --copy-to` now write a local `.replaytrc.toml` with explicit `target` and `inputs_file`, so freshly scaffolded workflows can use bare `replayt run --dry-check` / `replayt run` without repeating `workflow.py` and `inputs.example.json`; covered by updated CLI regressions and docs.
- **Enterprise integrator:** `replayt export-run` and `replayt bundle-export` now stamp `manifest.json` with a compact `run_summary` (workflow/version, contract digest from `run_started`, status, counts, tags, run metadata, experiment, and pending approvals), and `--target` now embeds `workflow.contract.json` so archived bundles can carry the exact workflow contract snapshot alongside the sanitized JSONL; `bundle-export --target` still adds `workflow.mmd.txt`, and the manifest records whether the exported contract matches the digest captured when the run started. Documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md), with expanded CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).

## 0.4.13 - 2026-03-22

- **Maintainer hygiene:** restore a non-empty `## Unreleased` section so `scripts/maintainer_checks.py --changelog-nonempty` and the pre-tag GitHub Actions verification can pass on the live repository again.

## 0.4.11 - 2026-03-22

- **Agent harness engineer:** `scripts/skill_release_loop.py` and `scripts/skill_release_loop_agent.py` now expose the resolved skill directory as both `{skill_root}` command placeholder and `SKILL_ROOT` env var so custom backends can consume the same contract as the built-in wrapper. `scripts/run_codex_skill.py` now accepts `--skill-root`, honors `SKILL_ROOT`, falls back to repo-local `.cursor/skills` when present, validates the directory loudly, and forwards it to `codex exec --skill-root`; covered by new regressions in [`tests/test_run_codex_skill.py`](tests/test_run_codex_skill.py), [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).
- **MCP / tooling:** version the remaining machine-readable CLI JSON payloads so wrappers can discover and validate them without scraping prose: **`replayt run --output json`** now emits **`replayt.run_result.v1`**, **`replayt inspect --output json`** emits **`replayt.inspect_report.v1`** (with `run_id`), **`replayt stats --output json`** emits **`replayt.stats_report.v1`**, and **`replayt diff --output json`** emits **`replayt.diff_report.v1`**. **`replayt version --format json`** now advertises those schema ids plus packaged-example JSON helpers under `cli_machine_readable_schemas`; documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md), with CLI regressions updated in [`tests/test_cli.py`](tests/test_cli.py).
- **Reviewcodebase pass:** `replayt doctor --format json` and `replayt config --format json` now run trust-boundary checks against the resolved raw `OPENAI_BASE_URL` while still printing only the sanitized label, so embedded query tokens and user-info credentials are warned about without leaking secret values; `scripts/skill_release_loop.py` and `scripts/skill_release_loop_agent.py` replace `assert proc.stdout is not None` with explicit `LoopError` handling when a child process stdout pipe is unavailable. Covered by updates in [`tests/test_cli.py`](tests/test_cli.py), [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).
- **Docs:** de-slop [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), [`docs/RELEASE_INVENTORY.md`](docs/RELEASE_INVENTORY.md), [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md): remove staged tutorial lead-ins and review-summary labels, replace a few contrast-heavy phrases with direct wording, and normalize broken punctuation to plain ASCII where it leaked into Markdown.
- **Improvedoc pass:** sanitize credential-bearing `OPENAI_BASE_URL` values before they reach `run_started.runtime.llm`, `llm_request` / `llm_response` effective payloads, and operator-facing `replayt config` / `replayt doctor` output; preserve an explicitly configured LLM gateway `base_url` even when `provider` is set so OpenAI-compatible proxies are not silently replaced by provider presets; and open `--sqlite` read paths in true read-only mode so `inspect` / `report` / `runs` / `stats` do not run SQLite init/WAL setup on nominally read-only commands. Covered by new regressions in [`tests/test_security.py`](tests/test_security.py), [`tests/test_runner.py`](tests/test_runner.py), [`tests/test_sqlite_store.py`](tests/test_sqlite_store.py), and [`tests/test_cli.py`](tests/test_cli.py); review artifact refreshed in [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md).
- **Design fidelity:** trusted CLI policy subprocesses (`run_hook`, `resume_hook`, `export_hook`, `seal_hook`) now leave compact audit breadcrumbs instead of staying invisible: successful runs record `run_started.runtime.policy_hooks`, `approval_resolved` stores `policy_hook` on gated resumes, export/seal manifests record `policy_hook`, and `replayt doctor --format json` adds a soft `policy_hooks_external_code` warning when those gates are configured; documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py) and [`tests/test_runner.py`](tests/test_runner.py).
- **Maintainer tooling:** extend the default `skill_release_loop` / `skill_release_loop_agent` archetype pipeline with **`feat_mcp_tooling`** and **`feat_agent_harness_engineer`** (plus `/createfeatures` twelve-archetype table); document default `DEFAULT_SKILLS` and `.cursor/skills` vs Codex `--skill-root` / `$CODEX_HOME/skills` in [`CONTRIBUTING.md`](CONTRIBUTING.md).
- **Framework enthusiast:** `replayt inspect`, `replayt runs`, and `replayt stats` now accept repeatable **`--note-kind KIND`** filters (exact match; OR semantics) for explicit `step_note.payload.kind` breadcrumbs such as `framework_summary` or `subrun_link`, so framework-in-a-step workflows stay queryable without grepping raw JSONL; documented in [`docs/CLI.md`](docs/CLI.md) and [`src/replayt_examples/README.md`](src/replayt_examples/README.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- Dummy changelog entry for testing the release loop.
- **Enterprise integrator:** `run_hook` now receives normalized JSON strings for the resolved run inputs, tags, run metadata, and experiment payload (`REPLAYT_RUN_INPUTS_JSON`, `REPLAYT_RUN_TAGS_JSON`, `REPLAYT_RUN_METADATA_JSON`, `REPLAYT_RUN_EXPERIMENT_JSON`) so org policy wrappers can veto runs based on change tickets, deployment tiers, or tag allowlists before replayt writes new events; documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), and [`docs/CONFIG.md`](docs/CONFIG.md), with expanded regression coverage in [`tests/test_cli.py`](tests/test_cli.py).
- **Startup IC:** `replayt run` / `ci` / `validate` / `doctor --target` / `try` now accept repeatable **`--input key=value`** flags for the common case where writing a full `--inputs-json` blob is overkill; dotted keys build nested objects (`issue.title=...`), JSON-style scalars/objects are coerced when possible (`2`, `false`, `["a"]`, `{...}`), and these overrides can layer on top of **`--inputs-json`**, **`--inputs-file`**, or packaged-example defaults; documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Open-source maintainer:** add [`scripts/example_catalog_contract.py`](scripts/example_catalog_contract.py) plus checked-in snapshot [`docs/EXAMPLE_CATALOG_CONTRACT.json`](docs/EXAMPLE_CATALOG_CONTRACT.json) so maintainers can diff the packaged `replayt try` catalog (`replayt_examples.list_packaged_examples()`) before a release; [`scripts/maintainer_checks.py`](scripts/maintainer_checks.py) now includes that contract by default, and maintainer docs in [`README.md`](README.md) / [`CONTRIBUTING.md`](CONTRIBUTING.md) show the direct `--check` flow; covered by expanded [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).
- **Product engineer:** `replayt report-diff` now accepts **`--format markdown`** in addition to HTML so PM/support handoffs can live in tickets, chat, or checked-in notes without an HTML attachment; the Markdown diff keeps repeated structured outputs and approval occurrences distinct (`Decision #2`, `ship #2`) and still surfaces run metadata, experiment context, and failure signals; documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **DevOps / SRE:** `replayt config` and `replayt doctor --format json` now preview env-driven CI artifact sinks for `REPLAYT_JUNIT_XML`, `REPLAYT_SUMMARY_JSON`, and `REPLAYT_GITHUB_SUMMARY` / `GITHUB_STEP_SUMMARY`, and `doctor` fails preflight when a configured artifact path is unwritable or a requested GitHub summary sink is missing; documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/RECIPES.md`](docs/RECIPES.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Security / compliance:** `replayt resume` can now require a non-empty approval justification via `--require-reason` or project config `approval_reason_required = true`; the same rule is enforced in `resolve_approval_on_store(...)`, surfaced in `replayt config --format json`, and called out as a soft audit-policy check in `replayt doctor --format json`; covered by expanded CLI, runner, and security regressions in [`tests/test_cli.py`](tests/test_cli.py), [`tests/test_runner.py`](tests/test_runner.py), and [`tests/test_security.py`](tests/test_security.py).
- **Testing:** Disable `rich` console output in test configuration to prevent string assertion failures in GitHub Actions.
- **Staff / platform:** `replayt contract` now has one explicit snapshot workflow for upgrade checks: **`--snapshot-out PATH`** writes the current `replayt.workflow_contract.v1` JSON to disk, and **`--check PATH`** compares the live workflow against a checked-in snapshot with unified diff text or machine-readable **`replayt.workflow_contract_check.v1`** JSON for CI drift gates; documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Junior onboarding:** `replayt init` and `replayt try --copy-to` now print copy-paste next steps instead of a generic `activate` placeholder: shell-specific venv activation lines, `replayt doctor --skip-connectivity --target ...`, `--inputs-file` run examples, and `--dry-run` first for LLM-backed templates/examples so the first local check works before provider setup; template strings and onboarding docs now match; covered in [`tests/test_cli.py`](tests/test_cli.py).
- **ML / LLM engineer:** `LLMSettings`, `OpenAICompatClient`, and `ctx.llm.with_settings(...)` / `complete_text(...)` / `parse(...)` now accept optional `extra_body={...}` JSON for OpenAI-compatible gateway fields that replayt does not model directly; reserved chat keys are rejected, values are normalized to JSON-serializable shapes, default extra-body keys show up on `run_started.runtime.llm`, and per-call values are logged under `effective` on `llm_request` / `llm_response`; documented in [`README.md`](README.md), [`docs/RECIPES.md`](docs/RECIPES.md), [`docs/SCOPE.md`](docs/SCOPE.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), with regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py), [`tests/test_llm_coercion.py`](tests/test_llm_coercion.py), and [`tests/test_runner.py`](tests/test_runner.py).

## 0.4.10 - 2026-03-22

- **Agent harness engineer:** `scripts/skill_release_loop.py` and `scripts/skill_release_loop_agent.py` now expose the resolved skill directory as both `{skill_root}` command placeholder and `SKILL_ROOT` env var so custom backends can consume the same contract as the built-in wrapper. `scripts/run_codex_skill.py` now accepts `--skill-root`, honors `SKILL_ROOT`, falls back to repo-local `.cursor/skills` when present, validates the directory loudly, and forwards it to `codex exec --skill-root`; covered by new regressions in [`tests/test_run_codex_skill.py`](tests/test_run_codex_skill.py), [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).
- **MCP / tooling:** version the remaining machine-readable CLI JSON payloads so wrappers can discover and validate them without scraping prose: **`replayt run --output json`** now emits **`replayt.run_result.v1`**, **`replayt inspect --output json`** emits **`replayt.inspect_report.v1`** (with `run_id`), **`replayt stats --output json`** emits **`replayt.stats_report.v1`**, and **`replayt diff --output json`** emits **`replayt.diff_report.v1`**. **`replayt version --format json`** now advertises those schema ids plus packaged-example JSON helpers under `cli_machine_readable_schemas`; documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md), with CLI regressions updated in [`tests/test_cli.py`](tests/test_cli.py).
- **Maintainer tooling:** extend the default `skill_release_loop` / `skill_release_loop_agent` archetype pipeline with **`feat_mcp_tooling`** and **`feat_agent_harness_engineer`** (plus `/createfeatures` twelve-archetype table); document default `DEFAULT_SKILLS` and `.cursor/skills` vs Codex `--skill-root` / `$CODEX_HOME/skills` in [`CONTRIBUTING.md`](CONTRIBUTING.md).
- **Reviewcodebase pass:** `coerce_temperature` and `coerce_timeout_seconds` now reject non-finite floats and values outside OpenAI-style chat bounds (temperature ``[0, 2]``; timeout must be ``> 0``) so bad YAML/env merges fail fast instead of confusing provider errors. Policy hooks and the parent-timeout `replayt run` child process use ``stdin=subprocess.DEVNULL`` so piped CI stdin cannot block those subprocesses. `replayt doctor --format json` and `replayt config --format json` run trust-boundary checks against the resolved raw `OPENAI_BASE_URL` while still printing only the sanitized label, so embedded query tokens and user-info credentials are warned about without leaking secret values; `scripts/skill_release_loop.py` and `scripts/skill_release_loop_agent.py` replace `assert proc.stdout is not None` with explicit `LoopError` handling when a child process stdout pipe is unavailable. Covered in [`tests/test_llm_coercion.py`](tests/test_llm_coercion.py), [`tests/test_cli.py`](tests/test_cli.py), [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py), and [`tests/test_skill_release_loop_agent.py`](tests/test_skill_release_loop_agent.py).
- **Docs:** de-slop [`docs/CLI.md`](docs/CLI.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), [`README.md`](README.md), [`src/replayt_examples/README.md`](src/replayt_examples/README.md), [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md), one Unreleased **Security / compliance** bullet in this file, plus [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), [`docs/RELEASE_INVENTORY.md`](docs/RELEASE_INVENTORY.md), [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md), [`docs/SCOPE.md`](docs/SCOPE.md), and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md): plainer sentences, fewer em-dash splices, drop staged tutorial lead-ins where they appeared, and normalize broken punctuation to plain ASCII where it leaked into Markdown.
- **Improvedoc pass:** cap numeric segment length when parsing **`min_replayt_version`** / **`replayt_release_tuple`** so malicious or accidental megabyte digit runs fail fast instead of building enormous integers; sanitize credential-bearing `OPENAI_BASE_URL` values before they reach `run_started.runtime.llm`, `llm_request` / `llm_response` effective payloads, and operator-facing `replayt config` / `replayt doctor` output; preserve an explicitly configured LLM gateway `base_url` even when `provider` is set so OpenAI-compatible proxies are not silently replaced by provider presets; open `--sqlite` read paths in true read-only mode so `inspect` / `report` / `runs` / `stats` do not run SQLite init/WAL setup on nominally read-only commands. Regressions in [`tests/test_version_compare.py`](tests/test_version_compare.py), [`tests/test_security.py`](tests/test_security.py), [`tests/test_runner.py`](tests/test_runner.py), [`tests/test_sqlite_store.py`](tests/test_sqlite_store.py), and [`tests/test_cli.py`](tests/test_cli.py); review artifact refreshed in [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md).
- **Design fidelity:** trusted CLI policy subprocesses (`run_hook`, `resume_hook`, `export_hook`, `seal_hook`) now leave compact audit breadcrumbs instead of staying invisible: successful runs record `run_started.runtime.policy_hooks`, `approval_resolved` stores `policy_hook` on gated resumes, export/seal manifests record `policy_hook`, and `replayt doctor --format json` adds a soft `policy_hooks_external_code` warning when those gates are configured; documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py) and [`tests/test_runner.py`](tests/test_runner.py).
- **Enterprise integrator:** project-configurable **`verify_seal_hook`** / **`verify_seal_hook_timeout`** and env **`REPLAYT_VERIFY_SEAL_HOOK`** / **`REPLAYT_VERIFY_SEAL_HOOK_TIMEOUT`** so org policy can run a trusted subprocess after **`replayt verify-seal`** digests match but before success output (non-zero exit fails the command; mismatch skips the hook); subprocess receives **`REPLAYT_RUN_ID`**, **`REPLAYT_LOG_DIR`**, **`REPLAYT_VERIFY_SEAL_MANIFEST`**, **`REPLAYT_VERIFY_SEAL_JSONL`**, **`REPLAYT_VERIFY_SEAL_SCHEMA`**, **`REPLAYT_VERIFY_SEAL_LINE_COUNT`**, and **`REPLAYT_VERIFY_SEAL_FILE_SHA256`**. **`replayt config --format json`** adds a **`verify_seal`** hook section. **`run_hook`** also receives normalized JSON strings for the resolved run inputs, tags, run metadata, and experiment payload (`REPLAYT_RUN_INPUTS_JSON`, `REPLAYT_RUN_TAGS_JSON`, `REPLAYT_RUN_METADATA_JSON`, `REPLAYT_RUN_EXPERIMENT_JSON`). Documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`README.md`](README.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Framework enthusiast:** **`replayt runs`** and **`replayt stats`** accept repeatable **`--structured-schema NAME`** (OR semantics) for **`structured_output`** / **`structured_output_failed`** payload **`schema_name`**, and **`replayt inspect`**, **`replayt runs`**, and **`replayt stats`** accept repeatable **`--note-kind KIND`** for **`step_note.payload.kind`** breadcrumbs such as `framework_summary` or `subrun_link`. Documented in [`docs/CLI.md`](docs/CLI.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); related out-of-scope asks logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Startup IC:** optional default inputs file via env **`REPLAYT_INPUTS_FILE`** or **`[tool.replayt] inputs_file`** / **`.replaytrc.toml`** when **`--inputs-json`** and **`--inputs-file`** are omitted on **`replayt run`**, **`replayt ci`**, **`replayt validate`**, and **`replayt doctor --target`** (CLI flags win; env overrides project config; **`replayt try`** unchanged). Repeatable **`--input key=value`** overrides layer on top of **`--inputs-json`**, **`--inputs-file`**, env/project defaults, or packaged-example defaults. **`replayt config`** reports **`run.default_inputs_file`** / **`run.default_inputs_file_source`**. Documented in [`README.md`](README.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/CLI.md`](docs/CLI.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Open-source maintainer:** add **`src/replayt/__main__.py`** so **`python -m replayt`** runs the same Typer CLI as the **`replayt`** console script when **`PATH`** does not expose **`Scripts/`** (containers, some user installs). Add [`scripts/example_catalog_contract.py`](scripts/example_catalog_contract.py) plus checked-in snapshot [`docs/EXAMPLE_CATALOG_CONTRACT.json`](docs/EXAMPLE_CATALOG_CONTRACT.json) so maintainers can diff the packaged `replayt try` catalog (`replayt_examples.list_packaged_examples()`) before a release; [`scripts/maintainer_checks.py`](scripts/maintainer_checks.py) now includes that contract by default. Documented in [`docs/CLI.md`](docs/CLI.md), [`README.md`](README.md), and [`CONTRIBUTING.md`](CONTRIBUTING.md); related out-of-scope asks logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py) and [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).
- **Product engineer:** **`replayt report-diff`** accepts **`--format html|markdown`** so PM/support can paste a two-run comparison into tickets, chat, or checked-in notes; Markdown keeps repeated structured outputs and approval occurrences distinct (`Decision #2`, `ship #2`) and still surfaces run metadata, experiment context, and failure signals (no third JSON summary schema). Documented in [`docs/CLI.md`](docs/CLI.md), [`README.md`](README.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); related out-of-scope ask logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **DevOps / SRE:** env **`REPLAYT_STEP_SUMMARY`** sets an explicit file path for the same markdown block as **`--github-summary`** when **`GITHUB_STEP_SUMMARY`** is unset (non-GitHub CI and local scripts); parent directories are created as needed, and **`GITHUB_STEP_SUMMARY` wins when both are set**. **`replayt config`** and **`replayt doctor --format json`** preview env-driven CI artifact sinks for `REPLAYT_JUNIT_XML`, `REPLAYT_SUMMARY_JSON`, and `REPLAYT_GITHUB_SUMMARY` / `GITHUB_STEP_SUMMARY`, and `doctor` fails preflight when a configured artifact path is unwritable or a requested GitHub summary sink is missing. Documented in [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/RECIPES.md`](docs/RECIPES.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); related out-of-scope asks logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **ML / LLM engineer:** `llm_response` events now include **`finish_reason`** from the first chat-completion choice (null when omitted) plus optional **`chat_completion_id`** and **`system_fingerprint`** when the provider returns them, so JSONL reflects stop vs length vs filter without a separate observability stack. Documented in [`README.md`](README.md) and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).
- **Security / compliance:** On POSIX, **`replayt doctor`** and **`replayt config --format json`** add soft **`trust_dotenv_other_readable`** / **`trust_dotenv_other_writable`** checks for discovered **`.env`** files (working directory and the directory containing **`pyproject.toml`** / **`.replaytrc.toml`** when present); only path and mode bits are inspected. File contents are never read. **`replayt resume`** can require a non-empty approval justification via **`--require-reason`** or project config **`approval_reason_required = true`**; the same rule is enforced in **`resolve_approval_on_store(...)`**, surfaced in **`replayt config --format json`**, and called out as a soft audit-policy check in **`replayt doctor --format json`**. Documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md); CI composition note under **Beyond core: enforce `.env` permissions in CI** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_security.py`](tests/test_security.py) and [`tests/test_cli.py`](tests/test_cli.py), plus expanded runner coverage in [`tests/test_runner.py`](tests/test_runner.py).
- **Junior onboarding:** `OpenAICompatClient` turns HTTP **401** from `/chat/completions` into explicit **`RuntimeError`** text that distinguishes an **unset** `OPENAI_API_KEY` (points at `replayt init` / **`.env.example`**, **`replayt doctor`**, and **`replayt run --dry-run`**) from a **set but rejected** key; documented in [`docs/QUICKSTART.md`](docs/QUICKSTART.md); regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py).
- **Staff / platform:** optional **`min_replayt_version`** in **`[tool.replayt]`** / **`.replaytrc.toml`** pins a minimum installed replayt release (numeric `major.minor.patch` prefix); most CLI commands fail fast when the package is too old, while **`replayt config`**, **`replayt version`**, **`replayt doctor`**, and **`replayt init`** stay available for diagnostics and upgrades. **`replayt config --format json`** surfaces **`min_replayt_version_satisfied`** and related fields; **`replayt doctor`** adds **`project_config_min_replayt_version`** as a hard health check when the constraint is set. Documented in [`docs/CONFIG.md`](docs/CONFIG.md) and [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_version_compare.py`](tests/test_version_compare.py) and [`tests/test_cli.py`](tests/test_cli.py).

## 0.4.7 - 2026-03-22

- **reviewcodebase pass:** `replayt doctor` no longer relies on `assert` before the optional `/models` connectivity probe (explicit `settings is None` guard so `python -O` cannot skip validation); `coerce_max_tokens_for_api` rejects non-finite floats and numeric strings (`inf` / `nan` / overflow-sized literals) with a clear `ValueError` instead of `OverflowError` or `int` conversion failures; covered in [`tests/test_llm_coercion.py`](tests/test_llm_coercion.py).
- **Docs:** de-slop [`README.md`](README.md), [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), and [`docs/RELEASE_INVENTORY.md`](docs/RELEASE_INVENTORY.md): drop LangGraph contrast padding, trim schema phrasing, replace em-dash chains with sentences or semicolons, swap “additionally” for plainer wording, and trade “robustness / production-ready” labels for concrete terms.
- **Improvedoc pass:** remove stray root-level **`print_cli_output.py`** debug script; [`scripts/maintainer_checks.py`](scripts/maintainer_checks.py) now raises **`FileNotFoundError`** / **`RuntimeError`** with paths when a bundled helper under **`scripts/`** is missing or unloadable instead of **`assert`**-ing; regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).
- **Design fidelity:** reserve CLI exit code **`2`** for approval-paused **`replayt run`** / **`replayt ci`** only; read-only commands (**`inspect`**, **`replay`**, **`report`**, **`report-diff`**, **`export-run`**, **`bundle-export`**, **`seal`**, **`verify-seal`**), missing-SQLite guards, and resume argv validation now exit **`1`** on unknown run ids or bad input so **`2`** stays unambiguous for human gates; documented in [`docs/CLI.md`](docs/CLI.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Framework enthusiast:** **`replayt runs`** and **`replayt stats`** accept repeatable **`--tool NAME`** (OR semantics) to keep runs that logged at least one **`tool_call`** with that exact payload **`name`**; documented in [`docs/CLI.md`](docs/CLI.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); related out-of-scope asks logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Enterprise integrator:** project-configurable **`seal_hook`** / **`seal_hook_timeout`** and env **`REPLAYT_SEAL_HOOK`** / **`REPLAYT_SEAL_HOOK_TIMEOUT`** so org policy can veto standalone **`replayt seal`** after the JSONL is read but before the manifest is written (subprocess receives `REPLAYT_RUN_ID`, `REPLAYT_LOG_DIR`, `REPLAYT_SEAL_JSONL`, `REPLAYT_SEAL_OUT`, `REPLAYT_SEAL_LINE_COUNT`; tarball exports still use **`export_hook`** only); surface effective values under **`seal`** in **`replayt config --format json`**; documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Startup IC:** optional **`TARGET`** for **`replayt run`** / **`replayt ci`** when **`REPLAYT_TARGET`** or **`[tool.replayt] target`** / **`.replaytrc.toml`** `target` is set (CLI argument still wins); **`replayt config`** reports **`run.default_target`** / **`run.default_target_source`**; documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); related out-of-scope asks logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Open-source maintainer:** add [`scripts/maintainer_checks.py`](scripts/maintainer_checks.py) to run **`version_consistency`**, **`changelog_unreleased`**, **`check_docs_index`**, and **`public_api_report`** in one shot (machine-readable **`--format json`**, schema **`replayt.maintainer_checks.v1`**, optional **`--changelog-nonempty`**, **`--verbose`**, and **`--skip-*`** for partial checkouts); document it in [`CONTRIBUTING.md`](CONTRIBUTING.md) and **Forks and release hygiene** in [`README.md`](README.md); log related out-of-scope asks in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).
- **Product engineer:** **`replayt report`** accepts **`--format html|markdown`** so PM/support can paste a run summary into tickets or chat without HTML; Markdown mirrors **`--style`** (stakeholder/support still omit tool and token sections) and adds a **`replayt resume TARGET …`** operator hint when the run is paused; documented in [`docs/CLI.md`](docs/CLI.md); composition note under **Beyond core: streaming, hooks…** in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); related out-of-scope asks logged in [`.cursor/skills/REJECTION_BLOCKLIST.md`](.cursor/skills/REJECTION_BLOCKLIST.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **DevOps / SRE:** optional env **`REPLAYT_CI_METADATA_JSON`** (JSON object) merges into **`replayt.ci_run_summary.v1`** as **`ci_metadata`** when **`--summary-json`** or **`REPLAYT_SUMMARY_JSON`** is set; invalid JSON or a non-object fails fast before the run. Documented in [`docs/CLI.md`](docs/CLI.md), [`docs/CONFIG.md`](docs/CONFIG.md), and [`docs/RECIPES.md`](docs/RECIPES.md); composition guidance for SARIF / vendor CI / Kubernetes in [`src/replayt_examples/README.md`](src/replayt_examples/README.md); regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **ML / LLM engineer:** `LLMSettings`, `OpenAICompatClient`, and `ctx.llm.with_settings` / `complete_text` / `parse` now accept optional OpenAI-style **`stop`** sequences (up to four non-empty strings, bounded length); values are coerced, forwarded to `/chat/completions` when set, logged under **`effective`** on `llm_request` / `llm_response`, and surfaced on `run_started.runtime.llm`; documented in [`README.md`](README.md), [`docs/SCOPE.md`](docs/SCOPE.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), with regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py), [`tests/test_llm_coercion.py`](tests/test_llm_coercion.py), and [`tests/test_runner.py`](tests/test_runner.py).
- **Security / compliance:** `replayt doctor --format json` and `replayt config --format json` now include **`credential_env`**: a fixed list of common LLM-related environment variable names with boolean **`present`** only (no values). `doctor` adds a soft **`credential_env_extra_providers`** check when non-`OPENAI_API_KEY` entries from that list are non-empty, since replayt's OpenAI-compat client does not read them; text `replayt config` prints a short **`credential_env_note`** in that case. Documented in [`README.md`](README.md) and [`docs/CLI.md`](docs/CLI.md), with regressions in [`tests/test_security.py`](tests/test_security.py) and [`tests/test_cli.py`](tests/test_cli.py).
- **Junior onboarding:** `inspect`, `replay`, `report`, `report-diff`, `export-run`, and `bundle-export` now print a short **`replayt runs --limit 10`** hint (with matching `--log-dir` / `--log-subdir` / `--sqlite` when you used them) when a run id has no JSONL events, exiting **`1`** so exit **`2`** stays reserved for approval pauses; documented in [`docs/CLI.md`](docs/CLI.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py) (unknown run id smoke) and [`tests/test_run_id_hints.py`](tests/test_run_id_hints.py).
- **Staff / platform:** `replayt config` and `replayt doctor` now surface unsupported keys from `[tool.replayt]` / `.replaytrc.toml` as `project_config.unknown_keys` (JSON) and a soft `project_config_unknown_keys` doctor check so hyphen/underscore typos and copy-paste mistakes are visible instead of silently ignored; documented in [`docs/CONFIG.md`](docs/CONFIG.md) and [`docs/CLI.md`](docs/CLI.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).

## 0.4.6 - 2026-03-21

- **reviewcodebase pass:** `Runner` now rejects `max_steps < 1` at construction time so `0` or negative values cannot fail every run on the first step boundary check; invalid JSON bodies from `/chat/completions` no longer embed a raw response preview in the raised `RuntimeError` (only `body_bytes=…`), reducing accidental leakage of provider error pages or model output into logs and CI output; covered by updates in [`tests/test_runner.py`](tests/test_runner.py), [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py), and [`tests/test_security.py`](tests/test_security.py).
- **Docs:** de-slop [`README.md`](README.md), [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), [`docs/COMPARISON.md`](docs/COMPARISON.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md): drop redundant scope padding, rename review headings that read like summary labels, fix hosted-stack copy so unset-provider defaults match the **ollama** preset, shorten the `ctx.llm.parse` aside, and replace infomercial-style tutorial lead-ins with direct wording.
- **Security / improvedoc pass:** sanitize **`OPENAI_BASE_URL`** in trust-boundary check **`detail`** strings so embedded userinfo is not echoed into **`replayt config --format json`** or doctor payloads; include **`version_report`** in **`replayt version --format json`** `cli_machine_readable_schemas` for symmetric schema discovery; expand [`tests/test_security.py`](tests/test_security.py) coverage for trust-boundary and redaction helpers.
- **Design fidelity:** update the repository root **`.env.example`** to match local-first defaults (commented Ollama-style `OPENAI_BASE_URL` / `REPLAYT_MODEL` / `REPLAYT_PROVIDER`) so it matches **`replayt init`** output and **`LLMSettings.from_env()`** when env vars are unset; remote OpenRouter-style values are opt-in via uncommented edits.
- **Lint:** wrap the default skill-invocation string in [`scripts/skill_release_loop_agent.py`](scripts/skill_release_loop_agent.py) so `ruff check src tests scripts` satisfies the line-length limit.
- **Framework enthusiast:** **`replayt inspect`** accepts repeatable **`--event-type`** so local JSONL can be narrowed to `step_note`, `tool_call`, `llm_request`, and other `type` values (OR semantics); full-run **`summary`** is unchanged while text and JSON **`events`** lists are filtered, and JSON adds **`event_type_filter`** when the flag is set; documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Enterprise integrator:** add project-configurable **`export_hook`** / **`export_hook_timeout`** and env **`REPLAYT_EXPORT_HOOK`** / **`REPLAYT_EXPORT_HOOK_TIMEOUT`** so org policy can veto **`replayt export-run`** and **`replayt bundle-export`** before archives are written (subprocess receives `REPLAYT_RUN_ID`, `REPLAYT_EXPORT_KIND`, paths, mode, seal flag, event count, and bundle report style); surface effective values under **`export`** in **`replayt config --format json`**; documented in [`docs/CLI.md`](docs/CLI.md) and [`docs/CONFIG.md`](docs/CONFIG.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Startup IC fast path:** **`replayt try --copy-to DIR`** copies a packaged catalog example into the given directory as **`workflow.py`** and **`inputs.example.json`** (optional **`--force`**, JSON schema **`replayt.try_copy.v1`** with **`--output json`**) so teams can fork a tutorial file without hand-copying from site-packages; implemented in [`src/replayt_examples/catalog.py`](src/replayt_examples/catalog.py) and [`src/replayt/cli/commands/run.py`](src/replayt/cli/commands/run.py), documented in [`docs/CLI.md`](docs/CLI.md) and [`src/replayt_examples/README.md`](src/replayt_examples/README.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Open-source maintainer:** add [`scripts/version_consistency.py`](scripts/version_consistency.py) to fail fast when **`pyproject.toml`** `[project].version` and **`src/replayt/__init__.py`** `__version__` disagree (machine-readable **`--format json`**, schema **`replayt.version_consistency.v1`**), document it in [`CONTRIBUTING.md`](CONTRIBUTING.md), align the package **`__version__`** with the current **`pyproject.toml`** pin, and cover it in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).
- **Product engineer / support triage:** **`replayt runs`** accepts repeatable **`--status completed|failed|paused|unknown`** (OR semantics) so PMs and on-call can filter local logs for approval backlogs and failures without ad-hoc `jq`; documented in [`docs/CLI.md`](docs/CLI.md) with a CI / notification pointer in [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md); covered by new CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **ML / LLM engineer:** `LLMSettings`, `OpenAICompatClient`, and `ctx.llm.with_settings` / `complete_text` / `parse` now accept optional OpenAI-style **`frequency_penalty`**, **`presence_penalty`**, and integer **`seed`** (omitted from HTTP JSON when unset); values are coerced, forwarded to `/chat/completions`, logged under **`effective`** on `llm_request` / `llm_response`, and surfaced on `run_started.runtime.llm`; documented in [`README.md`](README.md), [`docs/SCOPE.md`](docs/SCOPE.md), and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), with regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py), [`tests/test_llm_coercion.py`](tests/test_llm_coercion.py), and [`tests/test_runner.py`](tests/test_runner.py).
- **Security / compliance:** add **`replayt verify-seal`** to compare live JSONL bytes against **`replayt.seal.v1`** or **`replayt.export_seal.v1`** manifests (with **`--manifest`** / **`--jsonl`** for extracted bundles), factor shared SHA-256 line/file digest computation for **`replayt seal`**, and surface soft POSIX warnings when **`log_dir`** is world-readable or world-writable in **`replayt doctor`** / **`replayt config --format json`**; documented in [`docs/CLI.md`](docs/CLI.md) and [`src/replayt_examples/README.md`](src/replayt_examples/README.md), with regressions in [`tests/test_cli.py`](tests/test_cli.py) and [`tests/test_security.py`](tests/test_security.py).
- **Junior onboarding:** `MODULE:VAR` target resolution now explains common import footguns (editable install, `PYTHONPATH` / `src/` layouts, using a `workflow.py` path, and `replayt doctor --target` for graph checks) and surfaces missing dependency modules when the package loads but its imports fail; documented in [`docs/CLI.md`](docs/CLI.md) and covered by new CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Staff / platform ergonomics:** add `replayt version` with `--format json`, emitting `replayt.version_report.v1` (package version, structured Python version, platform, and stable schema ids for other machine-readable CLI payloads) so CI and wrappers have one canonical compatibility probe; documented in [`docs/CLI.md`](docs/CLI.md) and covered by new CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Docs:** clarify in `QUICKSTART.md` how `ctx.llm.parse` automatically injects JSON Schema system prompts so users know they do not need to manually prompt for JSON structural constraints in their own messages.
- **Design fidelity:** switch the implicit default LLM preset from hosted `openrouter` to local `ollama` so `LLMSettings.from_env()`, `replayt config`, the generated `.env.example`, and the setup docs match the README's local-first principle; covered by updated LLM-settings regression coverage in [`tests/test_llm_settings.py`](tests/test_llm_settings.py).
- **Framework composition breadcrumbs:** add `RunContext.note(...)`, which records `step_note` events for small explicit framework/sub-run summaries inside a step, surfaces note counts in `inspect --output json`, and renders those notes in `replayt replay` / `report`; document the composition pattern in [`src/replayt_examples/README.md`](src/replayt_examples/README.md) and [`docs/RUN_LOG_SCHEMA.md`](docs/RUN_LOG_SCHEMA.md), with regressions in [`tests/test_runner.py`](tests/test_runner.py) and [`tests/test_cli.py`](tests/test_cli.py).
- **Enterprise policy gates and audit bundles:** add project-configurable `run_hook` / `run_hook_timeout` (plus `REPLAYT_RUN_HOOK{,_TIMEOUT}`) so org policy can veto `replayt run` / `ci` before new events are written, surface the effective run-hook settings in `replayt config --format json`, and let `replayt export-run` / `bundle-export` include `events.seal.json` via `--seal` for SHA-256 verification of the exported sanitized JSONL; covered by expanded CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Startup IC fast paths:** `replayt try` now lists and runs a curated packaged-example catalog via `--list` / `--example`, accepts `--inputs-json` or `--inputs-file` overrides for those samples, and `replayt init` now ships richer `issue-triage` and `publishing-preflight` starter templates so teams can scaffold closer-to-real workflows on day one; covered by expanded CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Maintainer release helpers:** add [`scripts/public_api_report.py`](scripts/public_api_report.py) to snapshot the top-level `replayt` export surface (including declared-but-missing names), [`scripts/check_docs_index.py`](scripts/check_docs_index.py) to verify `docs/README.md` coverage and README doc links, and [`scripts/changelog_unreleased.py`](scripts/changelog_unreleased.py) to extract or check the `## Unreleased` section in text/JSON form; covered by new regressions in [`tests/test_maintainer_scripts.py`](tests/test_maintainer_scripts.py).
- **Product-engineer reports:** `replayt report` adds `--style support` for failure / retry / parse-failure handoffs, report approval cards now surface `approval_resolved` audit fields (`resolver`, `reason`, `actor`) plus applied resume-path context when present, `report-diff` now compares run metadata / experiment / workflow metadata and failure signals, and `bundle-export --report-style support` carries the same handoff view into `report.html`; covered by expanded CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **DevOps / SRE operability:** `replayt ci` / `replayt run` write a machine-readable run artifact via **`--summary-json`** / **`REPLAYT_SUMMARY_JSON`** (schema **`replayt.ci_run_summary.v1`**) so CI wrappers can capture **`run_id`**, status, **`exit_code`**, the CLI **`target`**, resolved **`log_dir`**, optional **`sqlite`**, **`dry_run`**, and wall-clock **`duration_ms`** without scraping stdout; GitHub step summaries include **`duration_ms`** when **`--github-summary`** / **`REPLAYT_GITHUB_SUMMARY`** is set. `replayt doctor` can preflight-load **`--target`** plus **`--strict-graph`** / inputs parsing without executing a workflow, and both **`replayt doctor`** and **`replayt config --format json`** surface best-effort filesystem readiness for the resolved **`log_dir`** and optional SQLite mirror. Documented in [`docs/CLI.md`](docs/CLI.md), [`docs/RECIPES.md`](docs/RECIPES.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md); covered by expanded CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **LLM control surface:** `ctx.llm.complete_text(...)`, `ctx.llm.parse(...)`, and `ctx.llm.with_settings(...)` now support explicit `top_p` sampling plus per-call `provider` / `base_url` routing, `ctx.llm.parse(...)` can opt into native JSON-schema `response_format` via `native_response_format=True`, and parse failures now emit `structured_output_failed` events with stage/error metadata for JSONL debugging; covered by expanded LLM bridge, runner snapshot, and testing-client regressions in [`tests/test_llm_bridge.py`](tests/test_llm_bridge.py), [`tests/test_runner.py`](tests/test_runner.py), and [`tests/test_testing.py`](tests/test_testing.py).
- **Security audit controls:** `Runner` and the CLI now support repeatable `--redact-key` / project-configured `redact_keys` for case-insensitive structured-field scrubbing across logged payloads, `replayt resume` can enforce required approval actor fields via `approval_actor_required_keys` / `--require-actor-key`, and `run_started.runtime`, `replayt doctor`, and `replayt config --format json` now surface soft trust-boundary warnings for risky defaults such as `log_mode=full`, remote plain-HTTP base URLs, or embedded URL credentials; covered by new runner and CLI regressions in [`tests/test_runner.py`](tests/test_runner.py) and [`tests/test_cli.py`](tests/test_cli.py).
- **Junior onboarding:** `replayt init` now writes a template-aware `inputs.example.json` next to the scaffolded workflow and points next steps at `--inputs-json @inputs.example.json`; the CLI also accepts `--inputs-json @path.json` as a shell-safe file shortcut and Python file targets with exactly one top-level `Workflow` object even when it is not named `wf`; covered by expanded CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Config consistency:** project-configured `provider` and `model` now apply to CLI-created LLM clients the same way `log_dir` and `log_mode` already did, and the new `replayt config` command prints the effective defaults plus source provenance so teams have one canonical way to inspect what the CLI will do; covered by new CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **Contract visibility:** add `Workflow.contract()` plus `replayt contract TARGET` for snapshot-friendly workflow surfaces (states, expects, retries, declared edges), and include a safe runtime settings snapshot on `run_started` so archived JSONL shows which engine/LLM settings were in effect for the run; covered by new regressions in [`tests/test_runner.py`](tests/test_runner.py) and [`tests/test_cli.py`](tests/test_cli.py).

## 0.4.4 - 2026-03-21

- **SQLite robustness:** failed SQLite writes now roll back their transaction before re-raising, so a single insert failure no longer leaves the store wedged with `cannot start a transaction within a transaction` for the rest of the process; covered by new regressions in [`tests/test_sqlite_store.py`](tests/test_sqlite_store.py).
- **Read-path hardening:** read-only CLI commands now open JSONL stores without creating the log directory as a side effect, so `runs` / `stats` / `inspect` / `report` style commands do not mutate the filesystem just to discover that no logs exist; covered by a new regression in [`tests/test_cli.py`](tests/test_cli.py).
- **Docs:** refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with the current standalone review pass, including the SQLite rollback finding, the read-side log-directory creation finding, mitigation mapping, and targeted verification status.
- **CLI hardening:** project-configured `log_dir` and `sqlite` paths are now resolved relative to the config file that declared them, so runs launched from subdirectories do not silently write logs or SQLite mirrors into the wrong working directory; covered by new CLI regressions in [`tests/test_cli.py`](tests/test_cli.py).
- **CLI correctness:** `replayt run --dry-run --output json` now emits pure JSON instead of prepending the human banner line, so machine-readable output remains parseable in scripting and CI flows; covered by a new regression in [`tests/test_cli.py`](tests/test_cli.py).
- **Docs:** refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with the current standalone review pass, covering config-relative path resolution for `log_dir` / `sqlite`, the `--dry-run --output json` contract bug, mitigations, and targeted verification status.
- **Read-path hardening:** `JSONLStore.load_events()` now opens run logs in read-only mode, so read-only commands such as inspect/report/replay/export continue to work when JSONL logs are mounted or archived without write permissions; covered by a new regression in [`tests/test_jsonl_store.py`](tests/test_jsonl_store.py).
- **Maintainer tooling hardening:** [`scripts/check_changelog_if_needed.py`](scripts/check_changelog_if_needed.py) and [`scripts/skill_release_loop.py`](scripts/skill_release_loop.py) now mark the repository as a Git `safe.directory` for their internal git calls, which keeps the PR changelog gate and local release loop working in sandboxed or shared-owner checkouts; covered by new regressions in [`tests/test_changelog_gate.py`](tests/test_changelog_gate.py) and [`tests/test_skill_release_loop.py`](tests/test_skill_release_loop.py).
- **Docs:** refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with the current standalone review pass, including the read-only JSONL finding, the Git safe-directory tooling finding, mitigation mapping, and targeted verification status.
- **Docs portability:** normalize command-heavy punctuation in [`README.md`](README.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), [`docs/CLI.md`](docs/CLI.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), [`docs/STYLE.md`](docs/STYLE.md), and [`pyproject.toml`](pyproject.toml) so stock Windows terminals do not render the primary onboarding and maintainer docs as mojibake; add an explicit ASCII-first note for shell-facing docs.
- **Docs:** index the standing review record from [`docs/README.md`](docs/README.md) and refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with the current standalone review pass, including the documentation portability finding, mitigation, and targeted verification status.
- **Dry-run correctness:** `DryRunLLMClient` now respects common JSON Schema constraints such as `minLength`, numeric minima, `multipleOf`, and `minItems` when synthesizing structured placeholder data, so offline runs no longer fail valid constrained `ctx.llm.parse(...)` schemas; covered by a new constrained-schema regression in [`tests/test_testing.py`](tests/test_testing.py).
- **Graph export correctness:** Mermaid graph output now escapes quoted state names consistently in both [`src/replayt/graph_export.py`](src/replayt/graph_export.py) and [`src/replayt/notebook.py`](src/replayt/notebook.py), which fixes broken CLI/export and Jupyter diagrams for workflows whose state labels contain quotes; covered by new regressions in [`tests/test_graph_export.py`](tests/test_graph_export.py) and [`tests/test_notebook.py`](tests/test_notebook.py).
- **Docs:** refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with the current standalone review pass, including the dry-run constrained-schema finding, the Mermaid label-escaping finding, mitigation mapping, and current full-suite verification status.
- **Notebook graph correctness:** make [`src/replayt/notebook.py`](src/replayt/notebook.py) reuse the collision-resistant Mermaid node IDs from [`src/replayt/graph_export.py`](src/replayt/graph_export.py) so distinct state names such as `a-b` and `a_b` render as separate nodes in Jupyter; covered by a new notebook regression in [`tests/test_notebook.py`](tests/test_notebook.py).
- **Docs:** refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with the current standalone review pass, including the notebook graph-ID finding, updated evidence, and current verification status.
- **Roadmap docs:** refresh [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md) with a fresh `createfeatures` pass: ten new archetype-driven backlog ideas including contract snapshots, guided scaffolds, redaction previews, stale-run ops views, and composition-only guidance for prompt/provider sweeps and framework-hosted planner loops.
- **Roadmap docs:** refresh [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md) with another `createfeatures` pass: ten archetype-driven backlog ideas covering workflow policy profiles, shell-aware onboarding packets, seal verification, paused-run inventories, typed `resume_hook` contracts, and explicit composition-only guidance for eval-style sweeps and framework-hosted planners.
- **Docs:** de-slop [`README.md`](README.md), [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md), [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), [`docs/DEMO.md`](docs/DEMO.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), [`docs/PRODUCTION.md`](docs/PRODUCTION.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md) by replacing slogan-style lines, padded review prose, hedgey example phrasing, and one mojibake-affected command example with plainer wording.
- **Docs:** de-slop [`CONTRIBUTING.md`](CONTRIBUTING.md), [`docs/README.md`](docs/README.md), [`docs/INSTALL.md`](docs/INSTALL.md), [`docs/RECIPES.md`](docs/RECIPES.md), [`docs/SCOPE.md`](docs/SCOPE.md), and [`docs/COMPARISON.md`](docs/COMPARISON.md), plus tighten a few remaining headings and lead-ins in [`README.md`](README.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), [`docs/DEMO.md`](docs/DEMO.md), and [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) by removing summary-label headings, staged lead-ins, and broken smart punctuation.

## 0.4.3 - 2026-03-21

- **CLI hardening:** write-path commands now close SQLite-backed stores deterministically after `replayt run` / `resume`, which fixes leaked file handles and locked `events.sqlite3` files on Windows, and CLI JSON-object options now fail with `BadParameter` instead of uncaught `JSONDecodeError`; covered by new CLI regressions for SQLite cleanup plus invalid `--inputs-json`, `--metadata-json`, `--experiment-json`, and `--actor-json`.
- **Roadmap docs:** refresh [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md) with a new `createfeatures` pass: ten fresh archetype-driven backlog ideas, updated in-core versus composition classifications, and explicit citations to the existing README/tutorial headings that already cover eval harnesses, enterprise approval brokers, and framework composition outside core.
- **LLM config hardening:** `REPLAYT_PROVIDER=anthropic` no longer points the OpenAI-compatible client at Anthropic's native `/v1` host by default. `LLMSettings.from_env()` now requires `OPENAI_BASE_URL` for that provider, `LLMSettings.for_provider("anthropic")` fails fast with guidance, and `replayt doctor` reports the misconfiguration cleanly instead of crashing; covered by new LLM settings and CLI doctor regressions.
- **Docs:** correct `strict_mirror` wording in [`docs/CONFIG.md`](docs/CONFIG.md), [`docs/PRODUCTION.md`](docs/PRODUCTION.md), and [`src/replayt/persistence/multi.py`](src/replayt/persistence/multi.py) so the docs match the implementation and existing tests: strict mode fails loudly, but it does not provide cross-store atomicity once the primary write has happened.
- **Docs:** de-slop [`README.md`](README.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md) by replacing filler lead-ins, summary-style headings, and repetitive "Expected outcome" phrasing with plainer wording.

## 0.4.2 - 2026-03-21

- **CLI hardening:** read-only `--sqlite` commands now fail fast instead of creating empty SQLite files when the requested database is missing, and `replayt gc --sqlite` applies the same guard; covered by new CLI regressions for missing-database reads and GC.
- **Docs:** de-slop [`README.md`](README.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), [`docs/README.md`](docs/README.md), [`docs/INSTALL.md`](docs/INSTALL.md), and [`docs/DEMO.md`](docs/DEMO.md) by replacing contrast-heavy lines, hedgey phrasing, and em-dash-heavy summaries with plainer wording.
- **CLI hardening:** timeout-wrapped runs now assign a `run_id` before spawning the child process so parent-side timeouts still append `run_interrupted` even when `--run-id` was omitted, and the timeout child import helper now detects `src/` layout checkouts when preparing `PYTHONPATH`; covered by new CLI regressions for timeout interruption logging and `subprocess_env_child()`.
- **Roadmap docs:** refresh [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md) with a new `createfeatures` pass: ten updated archetype-driven backlog ideas, current in-core versus composition classifications, and explicit pointers to the existing README/tutorial headings that already cover eval harnesses, enterprise approval wrappers, and framework composition outside core.
- **Docs:** refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with the current standalone review verdict after fixing timeout subprocess observability and local `src/` checkout import handling, plus updated targeted pytest status.

## 0.4.1 - 2026-03-21

- **Review fixes:** `replayt report` / `report-diff` now preserve repeated approval occurrences and repeated structured outputs with the same schema name, `replayt gc --sqlite` also removes SQLite-only runs, Mermaid graph exports avoid node-ID collisions for distinct state names, the PR changelog gate fails closed when `git diff` cannot determine changed files, and `scripts/skill_release_loop.py` now matches the documented default skill order; covered by expanded CLI, graph-export, changelog-gate, and release-loop tests.
- **Docs:** de-slop [`README.md`](README.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), [`docs/COMPARISON.md`](docs/COMPARISON.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), [`docs/PRODUCTION.md`](docs/PRODUCTION.md), [`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md), [`docs/SCOPE.md`](docs/SCOPE.md), [`docs/DEMO.md`](docs/DEMO.md), [`docs/INSTALL.md`](docs/INSTALL.md), and [`src/replayt_examples/README.md`](src/replayt_examples/README.md) by replacing slogan-style lines, contrast-heavy framing, and a few em-dash-driven sentences with plainer wording.
- **Docs:** refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with a current standalone review pass covering `report-diff` output collapse, approval-diff audit gaps, the changelog-gate fail-open path, the skill-loop order mismatch, local repro notes, and the current `python -m pytest -q` status.
- **Runtime hardening:** redacted and `structured_only` exports now scrub `context_snapshot.data`, hook exceptions still emit `run_failed` plus terminal `run_completed`, OpenAI provider defaults use `gpt-4o-mini`, `OpenAICompatClient` accepts injected HTTP clients for deterministic tests and custom transports, `--dry-run` structured parsing synthesizes required schema fields, `replayt runs` orders by last event timestamp, `replayt diff` preserves repeated `structured_output` events with the same schema name, and `replayt doctor --format json` no longer marks the optional YAML extra as a hard health failure; covered by expanded CLI/runtime/export tests.
- **Docs:** trim rhetorical or pitch-style prose in [`README.md`](README.md), [`CONTRIBUTING.md`](CONTRIBUTING.md), [`docs/QUICKSTART.md`](docs/QUICKSTART.md), [`docs/COMPARISON.md`](docs/COMPARISON.md), [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), [`src/replayt_examples/README.md`](src/replayt_examples/README.md), and the `replayt init` scaffold strings in [`src/replayt/cli/constants.py`](src/replayt/cli/constants.py) / [`src/replayt/cli/templates.py`](src/replayt/cli/templates.py); flatten contrast framing, slogan-style wording, and em-dash-heavy phrasing without changing technical meaning.
- **Docs:** add [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md), a deep standalone code review covering export redaction leaks, hook failure audit gaps, provider preset correctness, dry-run structured parsing, and CLI diff/run-listing issues.
- **Docs:** refresh [`docs/CODE_REVIEW_2026-03-21.md`](docs/CODE_REVIEW_2026-03-21.md) with a current standalone review pass, adding the `replayt doctor --format json` health regression, updated local reproductions, and the current `python -m pytest -q` status from this workspace.
- **Docs:** refresh [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md) so the ten archetype asks use current replayt nouns (`LLMBridge`, `ctx.llm.with_settings(...)`, `resume_hook`, `replayt bundle-export`, `replayt seal`, `replayt try`, `replayt ci`) and cite the exact existing README/tutorial composition headings that already satisfy deferred-feature follow-up.
- **Roadmap docs:** add [`docs/ARCHETYPE_FEATURE_BRAINSTORM.md`](docs/ARCHETYPE_FEATURE_BRAINSTORM.md) with ten replayt-shaped feature ideas from ten developer archetypes, classifying which asks fit core versus composition; index it in [`docs/README.md`](docs/README.md).
- **Docs:** rewrite the README intro and **[`docs/COMPARISON.md`](docs/COMPARISON.md)** around replay-first positioning, the **Why not LangGraph?** comparison, the core loop, and the demo captions.
- **Contributor automation:** add `scripts/skill_release_loop.py` plus `scripts/run_codex_skill.py` / `scripts/codex_local.ps1` so the repo can use a local Codex CLI install under `.replayt/tools/codex-cli`, run the Cursor skills in order, loop until checks pass, require a changelog update, bump the patch version, tag, and push; document the workflow in [`CONTRIBUTING.md`](CONTRIBUTING.md) and cover it with `tests/test_skill_release_loop.py`.

## 0.4.0 — 2026-03-21

- **LLM defaults (breaking):** With **`REPLAYT_PROVIDER`** unset, **`LLMSettings` / `from_env`** target **OpenRouter** at `https://openrouter.ai/api/v1` with model **`anthropic/claude-sonnet-4.6`** (OpenRouter slug), not OpenAI’s host or **`gpt-4o-mini`**. Set **`REPLAYT_PROVIDER=openai`** and/or **`OPENAI_BASE_URL`** to use OpenAI. **`openrouter`** preset uses the same default model. **`replayt doctor`** shows **`(unset, OpenRouter preset)`** when provider is unset; **`test_from_env_default_uses_openrouter`** locks in the default.
- **Docs and examples:** `LLMSettings` / **`for_provider`**, `.env.example`, RECIPES / EXAMPLES_PATTERNS / CONFIG examples, **`replayt_examples` e10**, **`docs/demo.svg`**, bundled sample JSONL, README / INSTALL / CLI / CONFIG / COMPARISON / SCOPE / tutorials. **[`docs/RELEASE_INVENTORY.md`](docs/RELEASE_INVENTORY.md)** is rebased on **`v0.4.0`** with the **0.3.0** line archived as a historical ledger.

## 0.3.0 — 2026-03-20

User-facing themes below map to commits since git tag **`v0.2.0`**; see [`docs/RELEASE_INVENTORY.md`](docs/RELEASE_INVENTORY.md) for the archived commit ledger covering **`v0.2.0` … `f4a676a`**.

- **Breaking:** the tutorial package on PyPI is now **`replayt_examples`** (was `examples`). Update targets: `replayt run replayt_examples.e01_hello_world:wf` (not `examples.*`).
- **CLI layout:** Typer app split into **`replayt.cli.commands`** (`run`, `inspect`, `export`, `doctor`) plus **`config`**, **`validation`**, **`stores`**, **`targets`**, **`run_support`**, **`ci_artifacts`**, **`display`**, **`constants`**; slim **`main`** registers commands and re-exports.
- **CLI commands:** **`replayt validate`** and **`replayt run --dry-check`** with JSON output (`replayt.validate_report.v1`); **`--inputs-file`** (UTF-8, mutual exclusion with **`--inputs-json`**); **`--strict-graph`** (multi-state workflows with no declared transitions are an error when strict; otherwise **`validate`** / **`dry-check`** emit a warning); **`--experiment-json`** on run (feeds `run_started.experiment` and LLM effective settings); **`runs`** / **`stats`** filters **`--experiment key=value`** and **`--run-meta`**; **`replayt try`** defaults offline (**`--live`** for real LLM); **`replayt ci`** with **`--junit-xml`**, **`--github-summary`** (paths only; **`REPLAYT_JUNIT_XML`** still supported for **`replayt run`**); **`replayt seal`**; **`replayt export-run`** (**`.tar.gz`** + sanitization + bundled log JSON Schema); **`replayt bundle-export`**; **`report-diff`** HTML; richer stakeholder **`replayt report`** (approvals, timestamps); **`replayt doctor --format json`** (`replayt.doctor_report.v1`, **`healthy`** + exit); **`replayt init --ci github`**; **`replayt resume`** **`--reason`**, **`--actor-json`**, **`--resolver`**, optional **`resume_hook`** / **`REPLAYT_RESUME_HOOK`** with default **120s** subprocess timeout (**`resume_hook_timeout`** / **`REPLAYT_RESUME_HOOK_TIMEOUT`**; ≤ 0 = unlimited).
- **Logs & metadata:** **`REPLAYT_LOG_DIR`**, **`--log-subdir`**, **`--metadata-json`**; validate that the workflow’s initial state is a declared step before run.
- **Runner & workflow:** optional **`Runner(..., before_step=..., after_step=...)`**; optional **`Workflow(..., llm_defaults=...)`** and **`meta["llm_defaults"]`** merged into **`LLMBridge`** defaults (omitted from logged **`workflow_meta`**); YAML infers **`note_transition`** edges from **`next`** / **`branch`** / **`approval`** for graph validation; **`run_started`** carries workflow meta; LLM **`with_settings(experiment=...)`**; **`ReplaytError`** (e.g. **`RunFailed`**) from a step does not consume retry attempts; approval resume pairs **`approval_resolved`** with **`approval_requested`** in **FIFO** order per id; per-run approval state reset; **`resolve_approval_on_store`** resolves the **oldest** pending request for that id; store records **`approval_resolved`** **`approval_id`** as string.
- **Persistence:** **`make_store`** used from timeout/interrupt paths so **`run_interrupted`** reaches SQLite mirrors when **`--sqlite`** is set; **`MultiStore.close()`** closes SQLite mirrors; **`strict_mirror`** defaults **true** whenever **`--sqlite`** (or config **`sqlite`**) is used unless **`strict_mirror`** is set explicitly; **`SQLiteStore`**: WAL mode, **`__enter__`/`__exit__`**, corrupt **`payload_json`** surfaced as **`RuntimeError`** with **`run_id`** and **`seq`**; JSONL tail fallback when the last line is unreadable; lock failures raise **`LogLockError`**; lock failures logged on POSIX and Windows; Windows empty-file lock sentinel; **`replayt seal`** validates **`run_id`** like stores and refuses JSONL paths outside the resolved log directory.
- **LLM & tools:** **`llm_coercion`** for temperature / max_tokens / timeout (YAML + **`LLMBridge`**); **`OpenAICompatClient`** reads chat completions with **streaming** and enforces **`LLMSettings.max_response_bytes`** (env **`REPLAYT_LLM_MAX_RESPONSE_BYTES`**); **`LLMSettings.from_env`** validates **`REPLAYT_LLM_MAX_RESPONSE_BYTES`** and **`REPLAYT_LLM_MAX_SCHEMA_CHARS`**; **`LLMBridge.parse`** rejects JSON Schemas over **`max_schema_json_chars`** (default 250k) and responses over **`max_parse_response_chars`** (default 4M); clearer error when the model returns no JSON object; HTTP retries treat **500** as retryable; **`_extract_json_object`** caps **`{`** scan work and, after dropping dict spans nested inside a larger dict, takes the **last** remaining span; **`coerce_max_tokens_for_api`** rejects booleans with **`TypeError`** (same idea as temperature); **`ToolRegistry`** resolves **`get_type_hints`** with the defining module’s globals, with **`Any`** fallback + warning on unresolved forward refs; YAML LLM prompts interpolate only **named placeholders** (valid identifiers); **`collect_report_context`** and **`build_run_report_html`** both use **`aggregate_run_report_data`** (single pass over events); float **`Retry-After`** parsing; **`DryRunLLMClient`** nested **`$ref` / allOf / oneOf`** minimal schema fill.
- **Fixes (after `v0.2.0` tag):** CLI tool/report/diff paths use **`name`** / **`arguments`** to match **`tools.py`** emissions; YAML init template uses **`next`** (not **`transition`**); **`Runner`** context manager closes **`httpx`**; lazy **`httpx.Client`** in **`OpenAICompatClient`** for dry/mock clients; deep-copy context in snapshots for pause/resume; **`diff`** gains **`--sqlite`**; **`MultiStore.append`** catches mirror failures like **`append_event`**; clip-path fix for demo SVGs so replay command text stays visible.
- **CI & tests:** PR gate requiring **`CHANGELOG.md`** when protected paths change; test that tutorial **`MODULE:wf`** targets from the examples README stay importable.
- **Documentation:** **[`docs/PRODUCTION.md`](docs/PRODUCTION.md)**, **[`docs/RECIPES.md`](docs/RECIPES.md)**, **[`docs/INSTALL.md`](docs/INSTALL.md)**, **[`docs/CLI.md`](docs/CLI.md)**, **[`docs/CONFIG.md`](docs/CONFIG.md)**, **[`docs/COMPARISON.md`](docs/COMPARISON.md)**, **[`docs/EXAMPLES_PATTERNS.md`](docs/EXAMPLES_PATTERNS.md)**; **[`docs/QUICKSTART.md`](docs/QUICKSTART.md)** and **[`docs/SCOPE.md`](docs/SCOPE.md)**; README slimming and onboarding (recipes extracted, comparison / install / report); quickstart “replay vs regenerate” diagram + PII note; PyPI **Beta** classifier; four animated SVG demos (core loop, approval gate, failure debug, framework comparison); README and tutorial README composition notes (streaming, planners, agent-framework boundaries); plainer wording in README, **`docs/README`**, **`docs/COMPARISON`**, tutorial README; **`MultiStore` / `strict_mirror`**, trusted **`resume_hook`**, and related notes in **`CONFIG.md`**, **`PRODUCTION.md`**, and code docstrings; maintainability: approval map rename in resume path; resume snapshot uses **`is not None`** so an empty string state name is respected; sanitized Mermaid node IDs in notebook helper.

## 0.2.0 — 2026-03-20

- Documentation: add [`docs/QUICKSTART.md`](docs/QUICKSTART.md) and [`docs/SCOPE.md`](docs/SCOPE.md); tighten README above-the-fold (comparison table, `pip install replayt`, `replayt report`, links); trim [`docs/DEMO.md`](docs/DEMO.md) to checklist; fix examples README link to [`docs/STYLE.md`](docs/STYLE.md); PyPI classifier **Beta**.
- HTML-escape all user-facing values in `RunResult._repr_html_` to prevent XSS in notebooks.
- Harden YAML workflow prompt interpolation against attribute-access injection (`format_map` replaced with safe formatter).
- Reuse `httpx.Client` across LLM calls for connection pooling and lower latency.
- Add opt-in HTTP retry via `LLMSettings(http_retries=N)` for transient errors (429, 502, 503, 504) with exponential backoff. Default is `0` (no retries) to stay explicit per design principle #2.
- Fix `DryRunLLMClient._minimal_json_from_schema` dead-code condition that included all fields regardless of `required`.
- Fix `tool-using` init template to match actual `ToolRegistry.register()` API.
- `MultiStore` mirror writes are now best-effort; failures are logged instead of crashing the run.

_Note:_ git tag **`v0.2.0`** points at commit `6a06bc9` (“bump version to 0.2.0”). Commit `8ceda2c` (“harden security… for 0.2.0”) is the next commit on `main` and is included in the notes above; finer-grained history is in [`docs/RELEASE_INVENTORY.md`](docs/RELEASE_INVENTORY.md).

## 0.1.0 — 2026-03-20

- Initial public skeleton: `Workflow` / `Runner`, JSONL + SQLite persistence, Typer CLI, three examples.
