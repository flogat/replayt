# UI / docs style (optional)

If you add a minimal web log viewer later, use **Tailwind CSS** for layout and typography. This repo's CLI and Markdown docs have no CSS requirements.

For command-heavy docs, prefer ASCII punctuation where practical (`"`, `'`, `...`, `->`) so examples stay readable in stock Windows terminals.
